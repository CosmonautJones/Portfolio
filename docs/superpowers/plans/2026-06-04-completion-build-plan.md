# Completion Build Plan (2026-06-04)

> Generated by the `completion-analysis` ultracode workflow (10 parallel analysts + synthesis chair). Sequenced, risk-rated.

## Sequenced plan

### #1 Dependency security remediation (npm audit fix + manual next 15.5.12→15.5.19 patch bump; NO --force, defer shadcn/MCP dev-only major)
- risk: **low** | go | playtest: false
- dependsOn: none
- rationale: VERIFIED low-risk and touches ZERO source files (package.json/lock only) — fully disjoint from every other item, so it gates nothing and nothing gates it. Do it first to establish a green, secure baseline before any feature churn enters the lockfile. Plain `npm audit fix` clears the critical vitest RCE + vite/undici/ws/postcss; manual next patch (NOT 16.x major) clears the only prod-reachable highs. Separating the safe patch wave from the deferred shadcn major directly honors the 'separate dependency major-bumps' constraint.

### #2 Frame interpolation (camera-only: lerp camera.y by alpha in buildScene, add Camera.prevY, plumb alpha through use-game-engine RAF loop)
- risk: **low** | go-with-playtest | playtest: true
- dependsOn: none
- rationale: VERIFIED: build-scene.ts currently returns state.camera BY REFERENCE with no alpha param, and the test asserts reference identity — this item rewrites buildScene's camera output into a NEW object and changes BuildSceneOptions. Every later renderer-feature (power-ups/weather/ghost render passes) reads buildScene output, so land the buildScene/RenderScene contract change FIRST while the scene object is still small, rather than rebasing it under power-up/weather/ghost diffs. Render-only (never writes back to state), low blast radius, well-specified recipe, and it is the single most-shared file other features extend. Pure-engine + math change → unit-testable, but camera feel on 120/144Hz changes, so a quick playtest confirms no rubber-band/judder regression.

### #3 Leaderboard scoring correctness — kill coin double-count (collision.ts killPlayer: finalScore = distance only; document score column = raw distance, coins live only in coin_bonus)
- risk: **medium** | go-with-playtest | playtest: false
- dependsOn: none
- rationale: VERIFIED at collision.ts:28 (totalScore = score + coinBonusScore) and game-scores.ts:29-37 (coins written into BOTH score and coin_bonus columns). This CHANGES STORED LEADERBOARD SEMANTICS — the explicit-caution constraint applies. Code fix is tiny and isolated (collision.ts + GameOverOverlay label + tests), but it must precede ghost-run because the ghost system's shouldUpdateGhost/markBeaten must agree on ONE score definition (distance vs distance+coins); locking 'raw distance' now prevents the ghost from being built on the wrong number. Combo stays cosmetic (no balance change). The destructive part — backfilling historical rows (UPDATE score = score - coin_bonus) — is a SEPARATE go/no-go decision requiring a product call + DB backup, NOT part of the code change. needsPlaytest=false (no gameplay feel change) but ship the code fix and DEFER the historical backfill pending explicit owner sign-off.

### #4 Wire dormant POWER-UP system (spawn in lanes, tick effects, shield in collision, slow-mo in obstacles, speed in player, fix getSpeedMultiplier stub, 2D+3D procedural render, callbacks)
- risk: **medium** | go-with-playtest | playtest: true
- dependsOn: Frame interpolation (camera-only: lerp camera.y by alpha in buildScene, add Camera.prevY, plumb alpha through use-game-engine RAF loop)
- rationale: First of the SEQUENTIAL gameplay chain. Mutates the shared tick.ts playing-phase pipeline (verified lines 214-229), lanes.ts/generateLanesIfNeeded, collision.ts (shield), obstacles.ts (slow-mo), player.ts (speed hop), AND adds a renderGhost-sibling render pass. Goes first in the chain because it is the most invasive (touches the most shared files) and fixes a real latent bug (getSpeedMultiplier stub returns 1 either way). Strong existing unit coverage; needs new integration tests (tick-level collect, shield-absorb, slow-mo slowdown, speed hop). Changes balance and feel → mandatory playtest. Build sequentially after interpolation so it extends the already-finalized buildScene/RenderScene contract.

### #5 Wire dormant WEATHER system (rain/fog/wind: updateWeather + applyWindDrift + applyRainSlide in tick/player, fix rainSlideApplied once-per-game bug, 2D fog overlay + rain particles, 3D THREE.Fog + rain points)
- risk: **medium** | go-with-playtest | playtest: true
- dependsOn: Wire dormant POWER-UP system (spawn in lanes, tick effects, shield in collision, slow-mo in obstacles, speed in player, fix getSpeedMultiplier stub, 2D+3D procedural render, callbacks)
- rationale: Second in the sequential chain — mutates the SAME tick.ts playing-phase block and player.ts landing path that power-ups just touched, so it MUST follow power-ups to avoid merge contention (the stated constraint). Carries a confirmed latent bug to fix during wiring: rainSlideApplied is set true once and never reset, so the slide fires at most once per entire game. rain-slide and wind-drift nudge the player laterally → directly affects fairness/feel near water and vehicles → mandatory playtest (confirm RAIN_SLIDE_DISTANCE 0.5 / WIND_DRIFT 0.3 don't cause unfair deaths). Renderer work (fog/rain) is additive to the contract interpolation already settled.

### #6 Wire dormant BOSS-LANE system (inject sections via generateLanesIfNeeded, thread callbacks through its signature, deterministic position-based checkBossClear, onBossStart/onBossClear, BOSS_CLEAR_BONUS via coinBonusScore)
- risk: **medium** | go-with-playtest | playtest: true
- dependsOn: Wire dormant WEATHER system (rain/fog/wind: updateWeather + applyWindDrift + applyRainSlide in tick/player, fix rainSlideApplied once-per-game bug, 2D fog overlay + rain particles, 3D THREE.Fog + rain points); Leaderboard scoring correctness — kill coin double-count (collision.ts killPlayer: finalScore = distance only; document score column = raw distance, coins live only in coin_bonus)
- rationale: Third in the chain — requires changing generateLanesIfNeeded's signature to (state, config, callbacks) and its single call site at tick.ts:224 (VERIFIED currently called without callbacks). lanes.ts is the same file power-ups extended for spawning, so sequencing after power-ups+weather avoids three concurrent edits to generateLanesIfNeeded. Recommend replacing the fragile score-heuristic checkBossClear with a stored end-Y position check so 'cleared' means actually traversed (and the bonus isn't awarded without crossing hazards). Renders with ZERO renderer changes (reuses generic lane/obstacle draw) — lowest rendering risk of the chain. Injects difficulty spikes → playtest the pacing/clearability of gauntlet/rapids/train_yard. Depends on the scoring fix only insofar as BOSS_CLEAR_BONUS flows through coinBonusScore, which after order 3 is cleanly separated from the leaderboard distance number.

### #7 GHOST-RUN system (record best run frames, persist to localStorage, replay translucent ghost in 2D+3D; add GameState.ghostTick/ghostPos + RenderScene.ghost)
- risk: **medium** | go-with-playtest | playtest: true
- dependsOn: Leaderboard scoring correctness — kill coin double-count (collision.ts killPlayer: finalScore = distance only; document score column = raw distance, coins live only in coin_bonus); Frame interpolation (camera-only: lerp camera.y by alpha in buildScene, add Camera.prevY, plumb alpha through use-game-engine RAF loop)
- rationale: Last of the engine-feature chain. Recommended seam drives recording from the use-game-engine RAF loop (minimal tick.ts coupling) but still adds GameState.ghostPos + RenderScene.ghost and a new render pass in BOTH renderers — so it extends the buildScene/RenderScene contract that interpolation finalized and power-ups/weather already grew. Depends on the scoring fix (order 3) to pin shouldUpdateGhost/markBeaten to the SAME score definition (raw distance) so the ghost doesn't vanish at the wrong moment. Touches death/persistence in onDeath alongside the scoring change, so sequence after it. No balance change (cosmetic overlay), but the ghost must visually track the live player correctly → playtest for alignment/judder, especially combined with interpolation.

### #8 Player Skin system (palette-remap cosmetics; per-skin 2D atlas variants, 3D material recolor, setSkin on both renderers, MenuOverlay picker, persist totalDeaths/totalDiamonds for unlocks)
- risk: **medium** | go-with-playtest | playtest: true
- dependsOn: GHOST-RUN system (record best run frames, persist to localStorage, replay translucent ghost in 2D+3D; add GameState.ghostTick/ghostPos + RenderScene.ghost)
- rationale: Touches the RENDERER atlas/material layer (sprite-atlas.ts, three-objects.ts createPlayer, MenuOverlay, GameCanvas) — largely DISJOINT from the tick.ts/lanes.ts/collision.ts engine chain, so it could run in a parallel worktree. Sequenced after the ghost item only because both add a 3D player-style mesh path (createPlayer/createGhost) and a setSkin/setStyle plumbing in three-renderer — doing skins after ghost avoids two simultaneous rewrites of the player-mesh factory. Blocking gap: 3 of 4 unlock conditions reference totalDeaths/totalDiamonds that are NEVER persisted today, so wire the stats helper or unlocks are unreachable. Cosmetic only (no balance) but ship static rainbow first (defer hue-cycling). Playtest the picker UX + that skins render in both 2D and 3D.

### #9 Challenge system wiring (instantiate tracker in use-game-engine, drive via callbacks, save+award XP on death, pass liveProgress to AdventureShell, add challenge XP keys)
- risk: **medium** | go | playtest: false
- dependsOn: GHOST-RUN system (record best run frames, persist to localStorage, replay translucent ghost in 2D+3D; add GameState.ghostTick/ghostPos + RenderScene.ghost)
- rationale: Tested library, currently unwired. Touches use-game-engine callbacks + AdventureShell + XP table — does NOT touch the tick.ts/lanes.ts/collision.ts engine math, so it is independent of the gameplay chain and shares only the use-game-engine callbacks object with power-ups/weather/boss/ghost (which append their own callback handlers). Sequence it after those so all callback additions to the single callbacks object land without conflict. No balance/feel change (progression/XP), so no gameplay playtest — but it awards XP and the open questions flag dedup + unauth handling, so verify XP keys and dedup rules before shipping. Lower priority than the visible game features.

### #10 Easter-egg & progression completeness (mount orphaned HiddenTerminal + Red Pill trigger, fire first_visit/first_steps, wire view_project/road_scholar, night_owl, cartographer threshold, pixel demo achievement)
- risk: **medium** | go | playtest: false
- dependsOn: none
- rationale: Entirely SITE-SIDE (layout, navbar, work pages, visitor-context, easter-egg registry) — fully disjoint from all game-engine and game-renderer files, so it can run in a parallel worktree at any time. Sequenced last only by priority, not dependency: it is the least load-bearing for 'top-grade completion' of the core game and carries open product questions (anonymous-visitor XP policy, intentional-vs-missing achievementIds, imperative-vs-evaluator model) that should be resolved before mass-wiring. Decisive recommendation: take the low-risk imperative path (explicit unlockAchievement calls + a cartographer counter), DEFER building a central condition-evaluator engine. No balance/feel change → no gameplay playtest; standard render/unit tests for each newly-wired path.

## Defer (do NOT do without explicit owner sign-off)

- Historical leaderboard backfill (UPDATE game_scores SET score = score - coin_bonus WHERE game_type='adventure' AND coin_bonus > 0): DEFER until an explicit product/owner decision on ranking semantics (raw-distance vs distance+coins) AND a DB backup + verification query. This is a destructive, irreversible-in-place mutation of stored competitive data and must not be bundled with the code fix. Exclude rows with coin_bonus=0 / pre-migration-012 rows to avoid over-correction.
- Combo-as-score-multiplier: DEFER / decline. Combo is currently cosmetic; making it reward score would silently change leaderboard balance and force a score re-baseline. Keep cosmetic unless product explicitly requests it with a designed multiplier curve.
- shadcn/MCP-SDK dev-only dependency major bumps (hono, fast-uri, flatted, path-to-regexp, express-rate-limit, brace-expansion, qs, ip-address): DEFER. Reachable only via the shadcn CLI at component-scaffold time, never in prod runtime. Do NOT run `npm audit fix --force` (would pull a Next major and churn the shadcn chain). Document as accepted risk or remove shadcn from devDependencies post-scaffold in a separate isolated change-set.
- Rainbow-skin animated hue-cycling: DEFER. Ship static palette first; render-time hue cycling needs a shader uniform or pre-baked frames — materially more work, not required for the cosmetic to function.
- Phase-2 entity (player/obstacle) frame interpolation: DEFER. Camera-only interpolation removes the dominant judder safely; per-entity interpolation must first guard hop-landing snap, log-id/teleport transitions, and lane-pruning of prev entities. Revisit only if sub-frame judder is still visible at 120/144Hz after camera-only ships.
- Central declarative achievement-condition evaluator: DEFER in favor of the existing imperative unlockAchievement model (lower risk, matches current code). Only build an evaluator if the team wants conditions to be data-driven; the imperative path covers every currently-dead achievement.
- Active power-up HUD/indicator (surfacing activePowerUps to RenderScene/GameEngineState) and per-skin GameOver/HUD pickers: DEFER as polish; silent gameplay effect + menu-only skin picker are acceptable for first cut.
- Anonymous-visitor XP/easter-egg policy: needs a product decision (currently awardXP/unlockAchievement no-op when !isAuthenticated, so logged-out visitors get nothing from eggs). Resolve before deciding scope of item 10.

## Parallelization

- SEQUENTIAL CHAIN (must be serialized — shared mutation of tick.ts playing-phase pipeline + lanes.ts/generateLanesIfNeeded + collision.ts/killPlayer + build-scene.ts/RenderScene): order 2 (interpolation, finalizes buildScene/RenderScene contract) → order 4 (power-ups: tick+lanes+collision+obstacles+player) → order 5 (weather: tick+player landing) → order 6 (boss: generateLanesIfNeeded signature+tick:224) → order 7 (ghost: RenderScene.ghost + onDeath persistence). These five edit the same hot files and the same render contract; building them in parallel worktrees would guarantee conflicts in tick.ts and build-scene.ts. Also note all of 4/5/6/7/9 append handlers to the SINGLE use-game-engine callbacks object — another shared-file reason to serialize.
- CAN RUN IN A PARALLEL WORKTREE vs the chain (disjoint files): order 1 (dependency bump — package.json/lock only, touches no source) and order 10 (easter-egg/progression — layout, navbar, work pages, visitor-context, easter-egg registry; zero game-engine/renderer overlap). Both are independent of the gameplay chain and of each other.
- SEMI-PARALLEL but sequenced for safety: order 3 (scoring fix — collision.ts killPlayer + GameOverOverlay + game-scores docs) touches collision.ts, which power-ups (order 4) also edits for the shield; could be done in parallel ONLY if merged before order 4 starts. Recommend landing order 3 first so power-ups extends the already-corrected killPlayer/onDeath path. order 8 (skins — sprite-atlas.ts, three-objects.ts createPlayer, MenuOverlay, GameCanvas) is renderer/UI-only and disjoint from the engine math, so it COULD run in parallel with the chain, but it shares the three-renderer player-mesh factory and setStyle plumbing with the ghost item (order 7) — keep it after ghost to avoid two concurrent rewrites of createPlayer/setStyle. order 9 (challenge) shares only the use-game-engine callbacks object with the chain; do it after the chain so callback additions don't conflict.

## Overall risk

Medium overall. No item is independently high-risk, but the aggregate risk is concentrated in (a) the four shared mutation points — tick.ts playing-phase pipeline, lanes.ts/generateLanesIfNeeded, collision.ts/killPlayer, build-scene.ts/RenderScene — which I verified are real chokepoints and which force the five game features into a strict sequential chain (parallelizing them WOULD be high-risk via merge conflicts), and (b) ONE stored-DB-semantics change (the verified coin double-count: collision.ts:28 writes distance+coins into the leaderboard score column AND coin_bonus separately). The code fix for the double-count is low-risk; the HISTORICAL BACKFILL is the only genuinely dangerous, irreversible operation and is explicitly deferred behind a product decision + DB backup. Dependency remediation is verified low-risk and isolated (no source changes, no --force, no Next major). Every gameplay-feel/balance-changing item (power-ups, weather, boss, ghost, skins, interpolation) is gated go-with-playtest per the 'feel needs a playtest' constraint; the scoring fix is go-with-playtest for the code only with the backfill deferred. Bar held at 'ship only verified, balanced, green': all five game features carry strong existing unit coverage but MISSING integration tests that must be added before each commit, and npm test + npm run build must pass per project workflow.

## Notes

Verified against source (not just the analyses): (1) collision.ts:28 confirms totalScore = state.score + state.coinBonusScore and game-scores.ts:29-37 confirms coins are stored in BOTH the score column and coin_bonus — the double-count is real and is a stored-leaderboard-semantics change requiring caution. (2) tick.ts:214-229 confirms the sequential playing-phase pipeline and that generateLanesIfNeeded(state, config) is called at line 224 WITHOUT callbacks — the boss-wiring signature change and its single call-site are confirmed. (3) build-scene.ts confirms scene.camera = state.camera is returned BY REFERENCE today with NO alpha param and a small RenderScene that already carries powerUps + weather — so the interpolation item legitimately owns the first edit to the shared scene contract, and the reference-identity test assertion will need to change to value-equality.

Key sequencing decisions and why: I put interpolation FIRST in the game chain (not power-ups) precisely because it rewrites the buildScene camera output and BuildSceneOptions — landing that contract change while RenderScene is still small avoids rebasing it under power-up/weather/ghost diffs. I put the scoring fix early (order 3) and ahead of ghost because ghost's shouldUpdateGhost/markBeaten must agree with the leaderboard on ONE score definition; locking 'raw distance' first prevents the ghost from being built on the wrong number. Power-ups → weather → boss is forced sequential by shared edits to tick.ts and (for power-ups+boss) lanes.ts/generateLanesIfNeeded.

Decisive recommendations baked into the plan: (a) leaderboard score column = raw distance, coins tracked only in coin_bonus; combo stays cosmetic; (b) boss checkBossClear should switch from the fragile score-heuristic to a stored end-Y position check; (c) weather wiring must fix the rainSlideApplied once-per-game latent bug; (d) power-ups must fix the getSpeedMultiplier stub (returns 1 in both branches today); (e) easter-egg work takes the imperative path, evaluator deferred; (f) skins ship static rainbow, persist totalDeaths/totalDiamonds (3 of 4 unlocks are otherwise unreachable).

Two truly parallel worktree opportunities to exploit: dependency bump (order 1) and easter-egg/progression (order 10) are both fully disjoint from the game chain and from each other — assign them to separate worktrees while the chain proceeds. Everything else should stay serialized on the chain or be sequenced as noted to avoid concurrent rewrites of generateLanesIfNeeded, the use-game-engine callbacks object, and the three-renderer createPlayer/setStyle factory.

---

# Detailed per-system analyses (recipes)

## Wire the dormant POWER-UP system into live gameplay (spawn, collect, tick effects, render in 2D + 3D, fire callbacks)  (wired: not, risk: medium)

**Current state:** The power-up module src/lib/game/power-ups.ts is fully written and unit-tested (src/lib/game/__tests__/power-ups.test.ts, ~30 passing assertions) but is COMPLETELY DORMANT: a repo-wide grep shows NO production file imports any of its exports (spawnPowerUpsForLane, checkPowerUpCollection, updatePowerUps, applyMagnetEffect, consumeShield, getSpeedMultiplier, getSlowMoMultiplier, prunePowerUps). What already exists: (1) Types are complete — PowerUp, ActivePowerUp, PowerUpType, GameState.powerUps/activePowerUps, and optional callbacks onPowerUpCollect/onPowerUpExpire in GameCallbacks (types.ts:107-122,181-183,203-204). (2) State is initialized — createInitialState and resetForNewGame both set powerUps:[] and activePowerUps:[] (tick.ts:187-188,314-315). (3) Tuning constants all exist (constants.ts:142-166): POWERUP_SPAWN_CHANCE, POWERUP_DURATION (shield=Infinity), POWERUP_COLLECT_RADIUS, POWERUP_MAGNET_RADIUS, POWERUP_SPEED_MULTIPLIER=0.67, POWERUP_SLOWMO_MULTIPLIER=0.6, POWERUP_PARTICLE_COLORS. (4) The scene contract ALREADY carries power-ups — RenderScene.powerUps exists (scene/types.ts:15) and buildScene copies state.powerUps into it (build-scene.ts:34). (5) The hook already declares the callbacks in its UseGameEngineProps shape via GameCallbacks but does NOT supply onPowerUpCollect/onPowerUpExpire in its callbacks object (use-game-engine.ts:219-372 — they are simply absent). What is MISSING: lanes.ts never calls spawnPowerUpsForLane (power-ups never appear); tick.ts never calls checkPowerUpCollection/updatePowerUps/applyMagnetEffect/prunePowerUps and never consumes shield in collision; NO renderer draws power-ups (neither sprites.ts 2D pass nor three-renderer.ts iterates scene.powerUps); the two effect-query stubs are inert — getSpeedMultiplier returns 1 in both branches (power-ups.ts:161-163) and getSlowMoMultiplier returns 0.6 but nothing calls it; updateObstacles (obstacles.ts:98) and updatePlayer hop timing (player.ts:146-156) ignore both multipliers; collision.ts:81-85 kills the player unconditionally with no shield check. There are also no power-up sprite assets in src/lib/game/sprites (grep found none), so 2D must draw power-ups procedurally with batch.drawQuad like coins' glow, and 3D must build a procedural mesh.

**Integration points:**
- src/lib/game/lanes.ts:generateLanes — after spawnDecorationsForLane(lane, config) (line ~193), add an optional powerUps array param (mirror the existing `coins?` param at line 124) and call spawnPowerUpsForLane(lane, config, nextId) pushing results into it; thread the new param through generateLanesIfNeeded (line ~220 call) and the two createInitialState/resetForNewGame calls in tick.ts (lines 142 and 279). NOTE: generateLanes is also called from collision/boss code paths — check all callers; simplest safe approach is to push into state.powerUps inside generateLanesIfNeeded after lanes are generated rather than deep-threading.
- src/lib/game/tick.ts:tick — inside the `if (state.phase === 'playing')` block (lines 214-229), add power-up calls in this order: after checkCoinCollection add `checkPowerUpCollection(state, config, callbacks)`; after updateObstacles/before/after updateCoins add `applyMagnetEffect(state, config)`; add `updatePowerUps(state, config, callbacks)` once per tick (timer decrement); after pruneLanesBehindPlayer add `prunePowerUps(state, pruneY)` using the same pruneY as pruneLanesBehindPlayer (player.gridPos.y + backDeathDistance + 5).
- src/lib/game/collision.ts:checkCollisions (lines 81-85) — before calling killPlayer for vehicle/train, call consumeShield(state); if it returns true, skip the kill (optionally spawn shield-break particles + continue), so shield absorbs one hit. Import consumeShield from ./power-ups.
- src/lib/game/obstacles.ts:updateObstacles (line 105) — multiply movement by the slow_mo factor: `obs.worldX += obs.speed * fixedTimestep * getSlowMoMultiplier(state)`. Import getSlowMoMultiplier from ./power-ups.
- src/lib/game/player.ts:updatePlayer (line 156) — apply speed power-up to hop progression: `player.hopProgress += fixedTimestep / (hopDuration * getSpeedMultiplier(state))` where getSpeedMultiplier must be FIXED to return POWERUP_SPEED_MULTIPLIER (0.67) when speed active (smaller hopDuration => faster hop). Import from ./power-ups.
- src/lib/game/power-ups.ts:getSpeedMultiplier (lines 161-163) — fix the stub: `return hasActivePowerUp(state, 'speed') ? POWERUP_SPEED_MULTIPLIER : 1;` and import POWERUP_SPEED_MULTIPLIER; change getSlowMoMultiplier hardcoded 0.6 to POWERUP_SLOWMO_MULTIPLIER for consistency.
- src/lib/game/renderer/passes/sprites.ts:SpritesPass.execute (line 73) — add a renderPowerUps(state, batch, whiteRegion) call (after renderCoins) that iterates state.powerUps, culls by screenY like renderCoins (lines 427-457), and draws a procedural icon per type using batch.drawQuad with POWERUP_PARTICLE_COLORS plus a pulsing glow (mirror coin bob/pulse). RenderState already includes powerUps so no type change needed there.
- src/lib/game/renderer/three-renderer.ts:ThreeRenderer.render (after syncCoins, line ~232) — add this.syncPowerUps(state): pool meshes by powerUp.id (new powerUpPool Map like coinPool), build via a new createPowerUp(type) factory, position worldX*PX_TO_WORLD + TILE_SIZE/2, -laneY*TILE_SIZE, with bob/spin; hide collected; cull via isLaneVisible(pu.laneY, camera); purge in the cleanup block (line 238-243) and destroy() (line 309-325).
- src/lib/game/renderer/three-objects.ts — add createPowerUp(type: string): THREE.Group factory (procedural, e.g. floating octahedron/box with per-type color from a colorMap mirroring createCoin lines 392-411); export it.
- src/hooks/use-game-engine.ts:callbacks object (lines 219-372) — add onPowerUpCollect: (type) => { audio.play...(); /* optional HUD pickup toast */ } and onPowerUpExpire: (type) => { /* clear HUD indicator */ }. Optionally surface activePowerUps to React state for a HUD by reading gameStateRef.current.activePowerUps in the loop (line 405-441).

**State/type changes:**
- No core type changes are strictly required — PowerUp/ActivePowerUp/GameState.powerUps/activePowerUps and the optional callbacks already exist in types.ts.
- If threading power-up spawning through generateLanes: add an optional `powerUps?: PowerUp[]` parameter to generateLanes in lanes.ts (parallel to the existing `coins?: Coin[]` param) OR avoid the signature change by pushing into state.powerUps directly inside generateLanesIfNeeded. The latter is lower-risk (no callers to update at lines 142/279 of tick.ts).
- renderer/render-pass.ts RenderState already declares powerUps (confirmed by the no-op uses in renderer.ts lines 219/244 that set powerUps:[]). Verify the field is non-optional readonly PowerUp[] there; if missing add it — but build-scene/scene-types already include it so it is almost certainly present.
- ThreeRenderer gains a private field `powerUpPool = new Map<number, PooledObject>()` plus its purge/destroy handling.
- Optional: add activePowerUps to GameEngineState in use-game-engine.ts if a HUD timer/indicator is desired (additive, non-breaking).

**Balance params:**
- POWERUP_SPAWN_CHANCE (constants.ts:142) shield 0.02 / speed 0.04 / magnet 0.03 / slow_mo 0.02 — per grass lane, at most one rolls (break after first hit). Sane: keeps power-ups rare; total ~11% of grass lanes carry one.
- POWERUP_DURATION: shield Infinity (consumed on hit), speed 5s, magnet 8s, slow_mo 6s — sane defaults already set.
- POWERUP_COLLECT_RADIUS 0.75*cellSize (~24px) — same as coins, sane.
- POWERUP_MAGNET_RADIUS 3 cells; magnet pull strength hardcoded 120*fixedTimestep in applyMagnetEffect (power-ups.ts:200) — tunable if pull feels weak.
- POWERUP_SPEED_MULTIPLIER 0.67 (hop duration multiplier => ~1.5x faster hops). POWERUP_SLOWMO_MULTIPLIER 0.6 (obstacles at 60% speed). Both defined but currently unused; wire them in.
- Open balance choice: should slow_mo also slow logs (water) and trains? Applying getSlowMoMultiplier uniformly in updateObstacles slows EVERYTHING including rideable logs — likely fine/desirable but worth confirming it doesn't make water trivially safe.

**Rendering needs:**
- 2D (SpritesPass): add renderPowerUps that draws each scene.powerUps entry as a procedural pulsing icon via batch.drawQuad (no sprite atlas asset exists). Mirror renderCoins bob (sin(animationTime*2.5 + id*0.7)) and glow pulse; color from POWERUP_PARTICLE_COLORS[type][0]. Cull by screenY against camera viewport. Skip collected. Call from SpritesPass.execute after renderCoins.
- 3D (ThreeRenderer): add syncPowerUps + a powerUpPool Map<number,PooledObject>, plus createPowerUp(type) in three-objects.ts (procedural mesh — e.g. spinning octahedron/box, per-type color). Position with PX_TO_WORLD, bob in Z, spin on Z, hide collected, cull via isLaneVisible. Wire into render() after syncCoins, into the periodic purge block, and into destroy().
- Optional but recommended: a player-state visual when shield/speed/slow_mo active — e.g. 2D ring around the lobster in renderPlayer when scene has active shield, and a screen tint for slow_mo. Requires passing activePowerUps into the scene (NOT currently in RenderScene) — would need RenderScene + buildScene additions. This is optional polish, not required for the core wiring.
- Optional HUD: active power-up timers rendered in React (GameCanvas overlay) by surfacing activePowerUps to GameEngineState.

**Test status:** Strong unit coverage already exists for the module in isolation: src/lib/game/__tests__/power-ups.test.ts covers spawning (grass-only, max one, valid types), collection (radius, dedup, particles, replacement), timer decrement/expiry/infinite-shield, consumeShield, hasActivePowerUp, magnet pull (and log-exclusion), and pruning. MISSING (must add for the wiring): (1) integration test that a full tick() with a power-up under the player collects it and populates activePowerUps and fires onPowerUpCollect; (2) collision test that an active shield absorbs a vehicle hit (player.alive stays true, shield removed, no onDeath); (3) updateObstacles test that slow_mo halves effective movement; (4) updatePlayer/getSpeedMultiplier test that speed shortens hop duration (and a unit test that the FIXED getSpeedMultiplier returns 0.67 when active, 1 otherwise — current stub returns 1 either way and would pass a naive test, so assert the 0.67 branch explicitly); (5) lanes generation test that grass lanes can populate state.powerUps. Renderers are not unit-tested (WebGL/Three) — follow existing convention of leaving renderer paths to the Playwright game-playtest pipeline. Per CLAUDE.md: run npm test + npm run build before commit.

**Recipe:**
1. 1. Fix the effect stubs in power-ups.ts: import POWERUP_SPEED_MULTIPLIER and POWERUP_SLOWMO_MULTIPLIER from constants; make getSpeedMultiplier return POWERUP_SPEED_MULTIPLIER when 'speed' active (else 1); make getSlowMoMultiplier return POWERUP_SLOWMO_MULTIPLIER when 'slow_mo' active (else 1). Add/adjust unit tests asserting the active branches.
2. 2. Spawn: in lanes.ts generateLanesIfNeeded (after pushing newLanes), or by threading a powerUps array through generateLanes, call spawnPowerUpsForLane(lane, config, nextId) for each new grass lane and push results into state.powerUps. Prefer pushing into state.powerUps inside generateLanesIfNeeded to avoid changing generateLanes' signature and all its callers (lower risk). Also seed in createInitialState/resetForNewGame if you want power-ups in the initial generated batch (optional).
3. 3. Tick pipeline (tick.ts, inside phase==='playing'): after checkCoinCollection add checkPowerUpCollection(state, config, callbacks); add applyMagnetEffect(state, config) (near updateCoins); add updatePowerUps(state, config, callbacks) once per tick; after pruneLanesBehindPlayer add prunePowerUps(state, player.gridPos.y + config.backDeathDistance + 5). Import all four from ./power-ups.
4. 4. Shield in collision.ts checkCollisions: before killPlayer in the AABB-hit branch, call consumeShield(state); if true, skip the kill (optionally spawn particles via existing particle helpers and continue scanning). Import consumeShield.
5. 5. slow_mo in obstacles.ts updateObstacles: multiply obs.worldX delta by getSlowMoMultiplier(state). Import it. Decide whether logs are also slowed (default: yes, uniform).
6. 6. speed in player.ts updatePlayer: divide hop advance by getSpeedMultiplier(state) (hopProgress += fixedTimestep / (hopDuration * mult)). Import it.
7. 7. 2D render: add renderPowerUps to SpritesPass (sprites.ts), called in execute() after renderCoins; draw procedural pulsing colored quads + glow from POWERUP_PARTICLE_COLORS, culled like coins, skipping collected. RenderState already exposes powerUps.
8. 8. 3D render: add createPowerUp(type) factory to three-objects.ts (export it); add powerUpPool + syncPowerUps to three-renderer.ts called after syncCoins; wire into the purge block and destroy().
9. 9. Callbacks: in use-game-engine.ts callbacks object, add onPowerUpCollect(type) (play a sound/toast) and onPowerUpExpire(type). Optionally surface activePowerUps to GameEngineState for a HUD.
10. 10. Add integration tests (tick-level collection, shield-absorb-hit, slow_mo obstacle slowdown, speed hop-duration). Run npm test and npm run build; both must pass before commit/push to main per project workflow.

**Open questions:**
- Should slow_mo also slow rideable logs and trains, or only road/train threats? Uniform slowdown in updateObstacles is simplest but slows logs too (could make water lanes easier).
- Should magnet also AUTO-COLLECT coins once pulled adjacent, or just pull them into the normal coin-collection radius (current checkCoinCollection handles pickup once close)? Pull-only relies on the existing coin collection radius firing.
- Should an active-power-up HUD/indicator be built now (requires surfacing activePowerUps to RenderScene and/or GameEngineState), or is silent gameplay effect acceptable for the first pass? Player has no feedback on remaining shield/speed/slow_mo time otherwise.
- Power-ups have no sprite/voxel assets — confirm procedural rendering (quads in 2D, simple mesh in 3D) is acceptable vs. authoring palette-indexed sprites in src/lib/game/sprites to match the coin/obstacle art pipeline.
- Should power-ups spawn in the initial pre-generated lanes (createInitialState) too, or only as the run progresses via generateLanesIfNeeded? Initial spawn could hand a shield at the start line.
- Should onPowerUpCollect award visitor XP / trigger achievements (there is an AchievementTracker and a separate visitor XP system), or stay purely in-game?

## Weather system (rain / fog / wind)  (wired: not, risk: medium)

**Current state:** The weather system is fully implemented in pure logic but DORMANT — never invoked from the tick loop and never drawn. What exists: (1) src/lib/game/weather.ts exports createWeather(), updateWeather(state,config,callbacks) [score-driven transitions with intensity lerp + onWeatherChange fire], applyRainSlide(state,config) [one-shot lateral slide after a hop], applyWindDrift(state,config) [per-tick accumulated lateral drift]. (2) Types in src/lib/game/types.ts: WeatherType='clear'|'rain'|'fog'|'wind', Weather{type,intensity,windDirection}, and GameState already carries weather, windDriftAccumulator, rainSlideApplied (all three initialized in tick.ts createInitialState ~L191-193 and reset in resetForNewGame ~L318-320). GameCallbacks has optional onWeatherChange?(weather). (3) Constants in src/lib/game/constants.ts L178-189: WEATHER_SCORE_THRESHOLDS (rain>=50, fog>=100, wind>=150), WEATHER_TRANSITION_DURATION=2, RAIN_SLIDE_DISTANCE=0.5, FOG_VISIBLE_LANES=4 (declared but UNUSED), WIND_DRIFT_PER_SECOND=0.3. (4) RenderScene (src/lib/game/scene/types.ts L18) already includes weather, and build-scene.ts L36 already copies state.weather into the scene — so BOTH renderers already receive weather every frame; they just ignore it. (5) Tests: src/lib/game/__tests__/weather.test.ts fully covers updateWeather/applyRainSlide/applyWindDrift in isolation. MISSING: tick.ts never calls updateWeather/applyWindDrift/applyRainSlide; use-game-engine.ts callbacks object (L219-372) has no onWeatherChange; neither renderer.ts (2D WebGL) nor three-renderer.ts (3D) reads scene.weather. Also note: rainSlideApplied is a single boolean that is set to true by applyRainSlide but is NEVER reset back to false on a new hop, so as written rain-slide fires at most ONCE per entire game — a latent bug that must be fixed during wiring.

**Integration points:**
- src/lib/game/tick.ts: tick() — inside the `if (state.phase === "playing")` block (currently L214-229), add `updateWeather(state, config, callbacks)` early (after updatePlayer, before updateObstacles) and add `applyWindDrift(state, config)` after updatePlayer. Import from "./weather".
- src/lib/game/player.ts: updatePlayer() landing branch (the `if (player.hopProgress >= 1)` block, ~L170-216) — call `applyRainSlide(state, config)` right after the landing-lane resolution; AND reset the per-hop flag `state.rainSlideApplied = false` at the START of a new hop. Cleanest reset site is initiateHop() in player.ts (~L58-66) where ridingLogId/hopProgress are already reset — set state is not available there (only player), so instead reset rainSlideApplied in tick.ts processActions before initiateHop, or in updatePlayer when hopProgress first exceeds 0. Recommended: reset state.rainSlideApplied=false in updatePlayer at the moment a hop completes is WRONG (it must be cleared before slide). Use: in tick.ts, clear state.rainSlideApplied=false when a new hopTarget is created. Simplest: clear it in player.ts updatePlayer right before calling applyRainSlide is too late; clear it when hop STARTS by passing state into initiateHop OR clearing in the `playing` action case in tick.ts processActions after initiateHop returns.
- src/hooks/use-game-engine.ts: callbacks object (L219-372) — add `onWeatherChange: (weather) => { audioRef.current?.play...; }` for an optional audio/visual cue, and optionally surface weather to React state for a HUD indicator.
- src/lib/game/renderer/renderer.ts: render(scene,_alpha) (L167-183) — add a renderWeather(scene) step after renderParticles and before endFrame; OR thread scene.weather into the composite/post pass. The endFrame()/renderBackground() currently hardcode `weather: {type:'clear',...}` in their synthetic RenderState (L220, L245) — pass real scene.weather through if a fog/post overlay shader is added.
- src/lib/game/renderer/three-renderer.ts: render(scene,_alpha) (L197-246) — read scene.weather and drive THREE.Fog/FogExp2 on this.scene.fog, a rain particle THREE.Points system, and a wind sway/scene.fog density.
- src/lib/game/renderer/passes/composite.ts (optional): add a fog/desaturation uniform driven by scene.weather for the 2D fog overlay, since CompositePass already receives RenderState.

**State/type changes:**
- No type changes strictly required — Weather, GameState.weather/windDriftAccumulator/rainSlideApplied, RenderScene.weather, and GameCallbacks.onWeatherChange all already exist.
- FIX the rainSlideApplied lifecycle: it is currently set true once and never cleared. Add a reset to false at hop start so the slide can fire once per hop. No new field needed; just an additional assignment site (tick.ts or player.ts).
- Optional: add a `weather` field to GameEngineState in use-game-engine.ts (L41-58) plus a useState if a HUD weather indicator is desired.
- Optional (rendering ergonomics): a small WEATHER_PARTICLE_BUDGET or rain-density constant in constants.ts; reuse the existing Particle type for 2D rain (no new type).

**Balance params:**
- WEATHER_SCORE_THRESHOLDS (constants.ts L179): rain>=50, fog>=100, wind>=150 — first weather appears ~score 50; reasonable. Consider gating ALL weather behind a higher first threshold (e.g. rain>=40) only if early game feels too busy.
- WEATHER_TRANSITION_DURATION=2s (L186): intensity lerps 0→1 over 2s and fades old weather out over 2s — smooth, keep.
- RAIN_SLIDE_DISTANCE=0.5 cells (L187): scaled by intensity, so max half-cell slide; gentle. Keep — anything >0.75 risks unfair water/vehicle deaths.
- WIND_DRIFT_PER_SECOND=0.3 cells/s (L189): scaled by intensity and windDirection; only applied when accumulator crosses 0.5 (weather.ts L153). At full intensity that is one half-cell nudge every ~1.7s. Keep low; >0.6 makes the player feel uncontrollable.
- FOG_VISIBLE_LANES=4 (L188): currently UNUSED — must be consumed by the fog renderer to limit visible depth (2D alpha gradient / 3D THREE.Fog near=player, far=player+4 lanes). This is the key fog balance knob.
- Intensity gating: applyRainSlide and applyWindDrift both require intensity>=0.3 (weather.ts L102/L135) so effects only kick in once weather is established — keep.
- Rain particle budget: respect the existing MAX_ATMOSPHERIC_PARTICLES=80 (constants.ts L135) if spawning 2D rain via the Particle array, or rain will starve grass/water ambient particles. Prefer a dedicated rain layer with its own cap (~40-60) rather than the shared budget.

**Rendering needs:**
- 2D (renderer.ts / WebGL2): RAIN — spawn diagonal streak particles (shape:'line', trail) biased by weather.windDirection, count scaled by weather.intensity, drawn via the existing GPUParticleRenderer in renderParticles(); spawn them in a new spawnRain() in particles.ts gated on scene.weather (or spawn in tick when weather.type==='rain'). FOG — add a fullscreen fog/desaturation overlay; either a new lightweight pass or a uniform in CompositePass (composite.ts) driven by intensity, with a vertical alpha gradient so only the nearest FOG_VISIBLE_LANES are clear and far lanes wash out. WIND — purely visual cue via existing decoration/grass shimmer plus the gameplay drift; optionally tint the rain streak angle. Note renderBackground()/endFrame() synthetic RenderState hardcodes clear weather (renderer.ts L220,L245) and must forward scene.weather if the fog overlay lives in composite.
- 3D (three-renderer.ts): FOG — set this.scene.fog = new THREE.Fog(bgColor, near, far) where near/far are derived from the player world Y and FOG_VISIBLE_LANES*TILE_SIZE, density scaled by weather.intensity; clear it (this.scene.fog=null) when type!=='fog'. RAIN — add a pooled THREE.Points rain system (vertical/diagonal lines) positioned around the camera focus, drift X by windDirection, opacity by intensity; create lazily and toggle .visible. WIND — sway tree meshes (treePool) and tilt rain by windDirection; optionally lerp scene.background / sun intensity to a greyer tone. All driven from the already-supplied scene.weather inside render() (L197); dispose the rain Points + clear fog in destroy().

**Test status:** Existing coverage is strong for the pure logic: src/lib/game/__tests__/weather.test.ts exercises createWeather, updateWeather transitions (clear/rain/fog/wind + gradual lerp + onWeatherChange), applyRainSlide (applies once, not twice, not in clear, not at low intensity), and applyWindDrift (accumulates, not in clear, not mid-hop, not at low intensity, threshold application). NEW tests needed: (1) tick.ts integration — after wiring, a tick at score>=50 eventually sets state.weather.type='rain' and intensity>0 (engine.test.ts style). (2) Per-hop rain-slide reset — assert rainSlideApplied flips back to false on a new hop so slide can re-fire (this guards the latent once-per-game bug). (3) applyWindDrift called from tick does not move the player mid-hop. (4) Renderer smoke tests (three-renderer.test.ts / renderer.test.ts) — rendering a scene with weather.type='fog'/'rain' does not throw and toggles scene.fog / rain visibility. No existing test asserts tick() invokes weather, so wiring is currently untested end-to-end.

**Recipe:**
1. 1. tick.ts: import { updateWeather, applyWindDrift } from "./weather". In tick()'s `if (state.phase==="playing")` block add `updateWeather(state, config, callbacks)` and `applyWindDrift(state, config)` (after updatePlayer, before updateObstacles).
2. 2. Fix the rain-slide lifecycle: reset `state.rainSlideApplied = false` whenever a new hop begins. Easiest correct site: in tick.ts processActions, in the 'menu' and 'playing' cases right after a successful initiateHop, set state.rainSlideApplied = false. Then call applyRainSlide(state, config) in player.ts updatePlayer inside the landing block (after the water/log resolution, ~L215), importing it from "./weather".
3. 3. Add weather.test additions: assert tick at score>=50 transitions to rain; assert rainSlideApplied resets on a fresh hop and slide can fire twice across two hops; assert wind drift via tick never fires mid-hop.
4. 4. use-game-engine.ts: add onWeatherChange to the callbacks object (L219) — minimally a no-op or audio cue (e.g. a soft thunder via audioRef). Optionally add `weather` to GameEngineState + useState and a HUD badge.
5. 5. 2D rain: add spawnRain(state, config) in particles.ts that, when state.weather.type==='rain' && intensity>=0.3, spawns diagonal line particles (shape:'line', trail:true) angled by windDirection, count ∝ intensity, with their own small cap; call it from tick.ts in the playing block. They render for free via existing renderParticles.
6. 6. 2D fog: add a fog uniform to CompositePass (composite.ts) or a new minimal overlay pass; forward real scene.weather through renderer.ts render() (and fix the hardcoded clear weather in renderBackground()/endFrame() synthetic states if the fog lives in composite). Drive a vertical alpha/desaturation gradient by intensity and FOG_VISIBLE_LANES.
7. 7. 3D fog: in three-renderer.ts render(), if scene.weather.type==='fog' set this.scene.fog = new THREE.Fog(0x1a1c2c, near, far) with near/far from camera focus + FOG_VISIBLE_LANES*TILE_SIZE scaled by intensity; else this.scene.fog=null.
8. 8. 3D rain: add a lazily-created pooled THREE.Points rain field around the camera focus in three-renderer.ts; toggle .visible and opacity by weather.type/intensity, drift by windDirection; dispose in destroy().
9. 9. 3D wind: sway treePool meshes and tilt rain by windDirection; optionally grey the background/sun by intensity.
10. 10. Run npx vitest run on weather/engine/renderer/three-renderer tests, then npm run build; commit & push to main.

**Open questions:**
- rainSlideApplied reset site: should it reset at hop START (so slide fires on the landing of that hop) — confirm the intended semantics. As written the flag is never reset; the recipe assumes 'one slide per hop'.
- Is weather meant to STACK or be exclusive? getTargetWeather returns a single type by highest threshold, so at score>=150 you only get wind (no rain+wind combo). Confirm exclusive is intended.
- Fog rendering approach for 2D: dedicated overlay pass vs. extending CompositePass — the latter reuses an existing pass but requires forwarding real weather through the two synthetic RenderStates in renderer.ts (L220/L245) that currently hardcode clear.
- Should onWeatherChange trigger audio (thunder/wind ambience) and a HUD indicator, or stay silent? audio.ts is procedural (no files) so a new sound would need a generator.
- Performance: 3D rain THREE.Points and 2D extra line particles add draw load — confirm acceptable on the existing mobile target, or cap density harder at low intensity.
- FOG_VISIBLE_LANES is declared but unused; confirm 4 lanes is the desired clear-vision radius before hard-coding it into both fog renderers.

## Wire the dormant BOSS-LANE system into the live game loop  (wired: not, risk: medium)

**Current state:** The entire boss-lane system is fully implemented but completely dormant — nothing in the runtime calls it. src/lib/game/boss-lanes.ts exports checkBossTrigger, generateBossSection, getBossSectionSize, and checkBossClear, plus three private generators (generateGauntlet = 3 dense road lanes at speedMultiplier 1.3 with car/truck obstacles whose speed is *1.5; generateRapids = 3 fast water lanes with short 2-cell logs, speed *1.8; generateTrainYard = 2 railroad lanes with staggered 4-cell trains). State fields exist (GameState.bossLanesUsed: BossPattern[] and inBossSection: boolean in types.ts), are initialized in createInitialState (tick.ts ~189-190) and reset in resetForNewGame (tick.ts ~316-317), but they are otherwise never read or mutated by the engine. Grep confirms boss-lanes.ts is imported ONLY by its own test file (src/lib/game/__tests__/boss-lanes.test.ts); the tick() loop in tick.ts (lines 201-246) never imports or calls any boss function. Constants exist in constants.ts: BOSS_LEVEL_TRIGGERS {gauntlet:2, rapids:4, train_yard:6}, BOSS_BUFFER_LANES=2, BOSS_CLEAR_BONUS=25. Scoring: score = (SAFE_START_LANES-1) - player.gridPos.y, set in player.ts updatePlayer on landing (~185-197); level via getLevelForScore against LEVEL_THRESHOLDS [0,25,50,100,150,200]. KEY DESIGN MISMATCH: checkBossTrigger keys off score (>= threshold && < threshold+3), but score is units of forward lanes (~one per hop), so thresholds 25/100/200 are reached only after dozens-to-hundreds of hops; the +3 window is the lanes the player advances before generation catches up. checkBossClear's clear condition (score >= threshold + bossSize + BOSS_BUFFER_LANES) is therefore a rough heuristic, not tied to actually traversing the injected lanes. Lanes are injected by appending to state.lanes and decreasing state.generatedUpTo; normal generation happens in lanes.ts generateLanesIfNeeded (called every tick from tick.ts:224) which appends lanes from generatedUpTo down to player.gridPos.y - generateAhead. Rendering is fully generic: buildScene (scene/build-scene.ts) passes state.lanes straight through to RenderScene; both the 2D and Three renderers draw lanes purely by lane.type and obstacle.type, all of which boss lanes reuse (road/water/railroad + car/truck/log/train). NOTE: lane.speedMultiplier (1.3/1.5 on boss lanes) is dead data — updateObstacles in obstacles.ts only reads obs.speed (obstacles.ts:105 obs.worldX += obs.speed * fixedTimestep); the speed boost is baked directly into obstacle.speed in the generators, so boss obstacles move correctly without any speedMultiplier wiring.

**Integration points:**
- src/lib/game/lanes.ts:generateLanesIfNeeded — primary injection site: after computing newLanes (or instead of a normal batch), call checkBossTrigger(state); if a pattern returns, call generateBossSection(pattern, startY, config, nextId, state.score), append those lanes, push pattern onto state.bossLanesUsed, set state.inBossSection=true, fire callbacks.onBossStart?.(pattern), and advance state.generatedUpTo past the injected block. Note: generateLanesIfNeeded currently has NO access to callbacks — signature is (state, config). Must thread GameCallbacks through.
- src/lib/game/tick.ts:tick (lines 214-229) — the playing-phase update list. generateLanesIfNeeded(state, config) is called at line 224 WITHOUT callbacks; change to pass callbacks. Add a checkBossClear(state, callbacks) call in the same block (e.g. right after generateLanesIfNeeded) so the clear bonus is evaluated each tick.
- src/lib/game/tick.ts imports (line 22) — add boss-lane imports (checkBossTrigger, generateBossSection, getBossSectionSize, checkBossClear) OR keep them encapsulated inside lanes.ts and only import checkBossClear into tick.
- src/hooks/use-game-engine.ts:callbacks object (lines 219-372) — add onBossStart and onBossClear handlers. Both are already declared optional in GameCallbacks (types.ts:205-206) but are NOT currently provided. Hook onBossStart to audio cue/screen-shake/banner; onBossClear to a bonus toast + audio. coinBonusScore already flows to UI via onCoinUpdate path and submitScore (use-game-engine.ts:323), so the BOSS_CLEAR_BONUS added to state.coinBonusScore will surface automatically.
- src/lib/game/__tests__/engine.test.ts and integration — add a tick-level test proving a boss section is injected when score crosses a trigger and cleared past it.

**State/type changes:**
- No new GameState fields strictly required — bossLanesUsed and inBossSection already exist and are init/reset. Optional: add a bossSectionEndY:number|null (or store the active BossLaneSection from types.ts, which is currently defined but UNUSED) to make checkBossClear deterministic (clear when player.gridPos.y < endY) instead of the score-heuristic.
- Change generateLanesIfNeeded signature in lanes.ts from (state, config) to (state, config, callbacks) so onBossStart can fire at injection time; update its single caller in tick.ts:224.
- Provide onBossStart/onBossClear in the callbacks object in use-game-engine.ts (types already allow them as optional).
- Consider deduping the trigger condition: checkBossTrigger uses score-based detection that can re-fire across the 3-unit window; guard injection with state.inBossSection===false so a section is not injected on top of itself.

**Balance params:**
- BOSS_LEVEL_TRIGGERS {gauntlet:2, rapids:4, train_yard:6} (constants.ts:169) — which LEVEL_THRESHOLDS index gates each boss; thresholds resolve to score 25 / 150 / 200.
- BOSS_BUFFER_LANES=2 (constants.ts:175) — safe grass lanes before the boss block; also reused in the clear math.
- BOSS_CLEAR_BONUS=25 (constants.ts:176) — added to coinBonusScore on clear.
- Baked obstacle speed multipliers in boss-lanes.ts: gauntlet *1.5, rapids *1.8, train default; vehicle density 3-4, log count 2-3, log width 2 cells, train width 4 cells. These are the real difficulty knobs (speedMultiplier on the lane is unused).
- Trigger window: score < threshold+3 in checkBossTrigger (boss-lanes.ts:44) — tune or replace with an inBossSection guard.
- Clear distance: threshold + bossSize + BOSS_BUFFER_LANES in checkBossClear (boss-lanes.ts:140).

**Rendering needs:**
- None required for basic functionality — buildScene (scene/build-scene.ts) passes state.lanes verbatim and both renderers draw by lane.type/obstacle.type, all of which boss lanes reuse, so injected boss lanes render with zero renderer changes.
- Optional polish: a visual telegraph for the buffer/warning lanes and a 'BOSS' banner. RenderScene (scene/types.ts) currently carries no boss flag, so any boss-specific visuals would need a new optional field on RenderScene (e.g. bossPattern?: BossPattern | null) populated in build-scene.ts from state.inBossSection, plus draw code in the 2D and Three renderers. Not needed to ship the mechanic.

**Test status:** "src/lib/game/__tests__/boss-lanes.test.ts gives strong UNIT coverage of all four exported functions: trigger detection per pattern + dedup + null case, section generation (lane counts, types, obstacle presence/types), getBossSectionSize, and clear detection (clears past section, no-op when not in section, no-op when score insufficient). What is MISSING is any INTEGRATION test proving the system actually runs inside tick()/generateLanesIfNeeded — because nothing calls it today. New tests needed: (1) ticking the engine until score crosses a trigger injects a boss section into state.lanes and sets inBossSection/bossLanesUsed/fires onBossStart; (2) advancing past it clears it, awards BOSS_CLEAR_BONUS to coinBonusScore, and fires onBossClear exactly once; (3) a pattern is never injected twice; (4) injection respects generatedUpTo so normal generation does not overwrite or duplicate. Per project TDD policy, run npm test + npm run build after wiring."

**Recipe:**
1. 1. Decide injection point: put the boss logic inside lanes.ts generateLanesIfNeeded (it already owns nextId/generatedUpTo bookkeeping). Change its signature to (state, config, callbacks).
2. 2. At the top of generateLanesIfNeeded, before normal generation, guard: if (!state.inBossSection) { const pattern = checkBossTrigger(state); if (pattern) { inject } }. Injection: nextId={value:state.nextEntityId}; const startY=state.generatedUpTo; const bossLanes=generateBossSection(pattern,startY,config,nextId,state.score); state.lanes.push(...bossLanes); state.nextEntityId=nextId.value; state.generatedUpTo = startY - getBossSectionSize(pattern); state.bossLanesUsed.push(pattern); state.inBossSection=true; callbacks.onBossStart?.(pattern);
3. 3. Let the existing normal-generation block run afterward so lanes keep generating ahead of the boss block (it generates from the new generatedUpTo down to player.y - generateAhead).
4. 4. In tick.ts, update the call site (line 224) to generateLanesIfNeeded(state, config, callbacks) and add the boss imports as needed.
5. 5. Add checkBossClear(state, callbacks) to the playing-phase block in tick.ts (after generateLanesIfNeeded). Strongly consider replacing the score heuristic in checkBossClear with a position check: store the boss section end-Y at injection (new state field or the existing BossLaneSection type) and clear when player.gridPos.y < endY. This makes 'cleared' mean 'actually traversed', and awards BOSS_CLEAR_BONUS to state.coinBonusScore + fires onBossClear.
6. 6. In use-game-engine.ts, add onBossStart (audio sting + triggerScreenShake + optional UI banner state) and onBossClear (bonus toast + audio). coinBonusScore already propagates to UI and submitScore, so no extra plumbing for the score bonus.
7. 7. (Optional) Add bossPattern to RenderScene in scene/types.ts + build-scene.ts and a banner/telegraph in both renderers for polish.
8. 8. Write integration tests in engine.test.ts (inject-on-trigger, clear-and-bonus, no-double-inject, generatedUpTo integrity). Run npm test and npm run build; commit to main per project workflow.

**Open questions:**
- Trigger semantics: checkBossTrigger keys off score (= forward lanes), so gauntlet fires at score 25, rapids at 150, train_yard at 200 — is that the intended pacing, or should triggers key off state.level directly (BOSS_LEVEL_TRIGGERS names suggest 'at level N')? The current score-window approach is fragile.
- Clear detection: keep the score-based heuristic (score >= threshold + bossSize + BOSS_BUFFER_LANES) or switch to a deterministic player-position check using a stored section endY? The heuristic can award the bonus without the player actually crossing the hazard lanes.
- Injection placement: injecting at generatedUpTo means the boss block appears ~generateAhead (30) lanes ahead of the player. Should the buffer/warning be closer so the telegraph is visible, and should there be an explicit 'warning' visual on the BOSS_BUFFER_LANES grass lanes?
- Re-trigger guard: confirm the inBossSection guard plus bossLanesUsed dedup fully prevents a pattern injecting twice within its 3-unit score window.
- Is onBossStart meant to also pause normal lane variety / force the section, or just overlay UI? Currently normal generation resumes immediately after the injected block.
- Should clearing a boss also award visitor XP / a game achievement (achievement-tracker.ts has no boss hook today)?

## Challenge system  (wired: partially, risk: medium)

**Current state:** Tested library, unwired

**Integration points:**
- instantiate tracker in use-game-engine
- drive tracker in callbacks pass coin type
- AdventureShell pass liveProgress
- thread callbacks via GameCanvas
- add challenge XP keys

**State/type changes:**
- add challengeProgress
- add tracker ref
- extend XP table

**Balance params:**
- daily 15 weekly 50, daily 3 weekly 1

**Rendering needs:**
- no renderer changes

**Test status:** Unit only need integration

**Recipe:**
1. add XP keys
2. instantiate tracker
3. wire callbacks and tick
4. save and award XP on death
5. pass liveProgress and add tests

**Open questions:**
- XP keys vs server award
- dedup
- unauth handling

## Player Skin system (palette-remap cosmetics for the lobster, 5 skins: default/golden/ghost/diamond/rainbow)  (wired: not, risk: medium)

**Current state:** The skin DATA LAYER is fully built and unit-tested but completely unwired to gameplay. What exists:
- `src/lib/game/types.ts:143-149` defines `SkinId` ("default"|"golden"|"ghost"|"diamond"|"rainbow") and `Skin { id, name, paletteOverrides: Record<number,number> }`.
- `src/lib/game/skins.ts` defines `SKINS` (palette-index remaps, e.g. golden maps 17->5, 18->42, 19->43, 88->13), `SKIN_UNLOCK_CONDITIONS` (golden=score>=200, ghost=deaths>=50, diamond=diamonds>=100, rainbow=allAchievements), and localStorage helpers `getSelectedSkin`/`setSelectedSkin` (key `adventure_skin`), `getUnlockedSkins`/`unlockSkin` (key `adventure_skins_unlocked`), plus `checkSkinUnlocks(stats: GameStats)`.
- `src/lib/game/__tests__/skins.test.ts` covers all of the above (definitions, conditions, persistence, unlock evaluation).

What is MISSING (nothing consumes any of it — grep for `Skin`/`paletteOverride` across `src` returns only types.ts, skins.ts, skins.test.ts):
- 2D/WebGL renderer never applies overrides. Lobster sprites are baked to RGBA ONCE at atlas-build time using a single static `PALETTE_RGBA` in `src/lib/game/renderer/webgl/sprite-atlas.ts:23`. Registration happens in `src/hooks/use-game-sprites.ts:39-44` via `spriteCache.prerender(key, pixels)` with no palette param. The player draw at `src/lib/game/renderer/passes/sprites.ts:525` always uses key `lobster_${facing}_${animation}` — one fixed look.
- 3D/Three.js renderer hardcodes player colors. `createPlayer()` in `src/lib/game/renderer/three-objects.ts:117` uses constant `COLORS.playerBody/playerEye/playerPupil/playerClaw` (lines 66-70). Built once in `three-renderer.ts:189`; `setStyle()` at `three-renderer.ts:253` is a no-op and there is no skin setter.
- No UI: `MenuOverlay.tsx` and `GameHUD.tsx` only have mute + 2D/3D toggle buttons; no skin picker.
- No stat tracking: only `adventure_high_score` is persisted (`use-game-engine.ts:192,298`). `totalDeaths` and `totalDiamonds` (required by `checkSkinUnlocks`) are NOT persisted anywhere, so 3 of 4 unlock conditions can never be satisfied as-is. `allAchievements` source also unconfirmed (achievement-tracker.ts exists but is not joined to skins).

**Integration points:**
- src/hooks/use-game-sprites.ts:39-44 — register skinned lobster variants: for the selected (or every unlocked) skin, call spriteCache.prerender(`${skinId}__${key}`, pixels) using a palette-overridden RGBA. Requires a new SpriteCache/atlas method that accepts paletteOverrides.
- src/lib/game/renderer/webgl/sprite-atlas.ts:138 addSprite + :53 renderPixelsToRGBA — add an overload/param to build a per-skin palette (remap index via paletteOverrides before PALETTE_RGBA lookup), and a new addSkinnedSprite on the atlas surfaced through SpriteCache.prerender.
- src/lib/game/renderer/passes/sprites.ts:525 — prefix the player spriteKey with the active skin: `${skin}__lobster_${facing}_${animation}` (fall back to unskinned key if region missing).
- src/lib/game/renderer/renderer.ts (GameRenderer) ~setSpriteStyle:148 — add setSkin(id) that stores skin and forwards to SpritesPass (mirror the setSpriteStyle plumbing); SpritesPass needs a setSkin/skin field.
- src/lib/game/renderer/three-objects.ts:117 createPlayer — accept a skin/color override arg and recolor body/claw/eye materials; map paletteOverrides (17->body, 19->claw, 88->eye) to hex via PALETTE.
- src/lib/game/renderer/three-renderer.ts:189 + :253 setStyle — add setSkin(id) that rebuilds/recolors this.playerMesh (remove old from scene, createPlayer(skin), re-add).
- src/components/adventure/MenuOverlay.tsx:15 — add a skin-picker row (props: unlockedSkins, selectedSkin, onSelectSkin), styled like the existing mute/palette buttons.
- src/components/adventure/GameCanvas.tsx:52,133,303 — add selectedSkin state initialized from getSelectedSkin(), pass to MenuOverlay, and on change call setSelectedSkin + rendererRef.current.setSkin + threeRendererRef.current.setSkin.
- src/hooks/use-game-engine.ts:280-303 (death/highscore persistence block) — increment+persist adventure_total_deaths on death and adventure_total_diamonds on diamond pickup, then call checkSkinUnlocks(stats) and surface newly unlocked skins.
- src/lib/game/collision.ts:29 / coin collection path — feed diamond-coin collection count into the persisted totalDiamonds stat.

**State/type changes:**
- Add `skin: SkinId` to the SpritesPass internal state and a `currentSkin` field on GameRenderer (renderer.ts).
- Add `setSkin(id: SkinId)` to GameRenderer (renderer.ts) and ThreeRenderer (three-renderer.ts), and the shared GameRenderer interface that both implement.
- Extend SpriteCache.prerender / SpriteAtlas.addSprite (sprite-cache.ts, sprite-atlas.ts) to accept an optional paletteOverrides map; add a buildPaletteRGBA(overrides) helper next to PALETTE_RGBA.
- createPlayer(skin?: Skin | colorOverride) signature change in three-objects.ts (default arg keeps existing tests/callers working).
- No GameState/engine type changes needed — skin is a pure render concern; selection/persistence lives in localStorage + React state, not in GameState.
- Optional: a GameStats persistence helper module (e.g. src/lib/game/stats.ts) for totalDeaths/totalDiamonds since none exists today.

**Balance params:**
- Unlock thresholds already defined in skins.ts SKIN_UNLOCK_CONDITIONS: golden score>=200, ghost deaths>=50, diamond diamonds>=100, rainbow allAchievements (default sane).
- rainbow skin currently has paletteOverrides as static base colors (17->3,18->4,19->2) with a comment that the renderer should cycle them at render time — decide whether to implement animated cycling (extra work) or ship static; default: ship static first.
- localStorage keys are fixed: adventure_skin, adventure_skins_unlocked; add adventure_total_deaths, adventure_total_diamonds for unlock stats.

**Rendering needs:**
- 2D: build per-skin RGBA copies of the 9 lobster frames (LOBSTER_SPRITES) + the 2 flipped left frames, registered under skin-prefixed keys; atlas size grows by ~11 sprites per skin (atlas auto-sizes, low risk).
- 2D: SpritesPass.renderPlayer must select the skin-prefixed key with graceful fallback to the unskinned key.
- 3D: recolor the player THREE.Group materials (body=index17, claw=index19, eye=index88 mappings) when skin changes; rebuild playerMesh on setSkin.
- Optional rainbow animation: per-frame hue cycle would require either pre-baking N rainbow frames or a shader uniform — out of scope for first cut.

**Test status:** Existing: src/lib/game/__tests__/skins.test.ts thoroughly covers the data layer (SKINS, conditions, getSelectedSkin/setSelectedSkin, getUnlockedSkins/unlockSkin, checkSkinUnlocks) — all green, no changes needed there. NO tests exist for skin application. New tests needed: (1) sprite-atlas/sprite-cache palette-override produces remapped RGBA (extend atlas-raw.test.ts or new test) — assert index 17 pixels become the override color; (2) createPlayer(skin) sets expected material colors (extend three-objects.test.ts which already asserts COLORS.playerBody); (3) GameRenderer.setSkin and ThreeRenderer.setSkin set/forward state (extend game-renderer.test.ts / three-renderer.test.ts); (4) GameCanvas/MenuOverlay picker renders unlocked skins and calls setSelectedSkin (React Testing Library); (5) stat-persistence increments totalDeaths/totalDiamonds and triggers checkSkinUnlocks.

**Recipe:**
1. 1. Add a palette-override path to the 2D pipeline: in sprite-atlas.ts add `buildPaletteRGBA(overrides: Record<number,number>)` (clones PALETTE_RGBA but reads palette[overrides[i] ?? i]) and `addSkinnedSprite(key, pixels, overrides, flipH)`; surface via SpriteCache.prerender optional 4th arg or a new prerenderSkinned method.
2. 2. In use-game-sprites.ts, after registering the base lobster frames, loop SKINS (or just unlocked+selected) and register `${skinId}__${key}` for all LOBSTER_SPRITES + LOBSTER_FLIP_KEYS dests using each skin's paletteOverrides. Skip default (no overrides) to save atlas space.
3. 3. Add `currentSkin` + `setSkin(id)` to GameRenderer (renderer.ts), forwarding to SpritesPass; give SpritesPass a `skin` field and a `setSkin` setter.
4. 4. In sprites.ts:525, compute `const base = `lobster_${facing}_${anim}`; const key = this.skin && this.skin !== 'default' ? `${this.skin}__${base}` : base;` with `atlas.has(key)` fallback to base.
5. 5. 3D: change createPlayer in three-objects.ts to accept an optional skin and resolve body/claw/eye/pupil hex from PALETTE via the skin's overrides (17,19,88 -> hex); keep default arg.
6. 6. Add `setSkin(id)` to three-renderer.ts that removes the old playerMesh, calls createPlayer(SKINS[id]), re-adds, and stores the id (rebuild on style switch too).
7. 7. Build a stats helper (src/lib/game/stats.ts) with get/increment for adventure_total_deaths and adventure_total_diamonds; wire increments into use-game-engine.ts death handler (~:280) and the diamond-coin collection path (collision.ts / coins).
8. 8. After each run, call checkSkinUnlocks({highScore, totalDeaths, totalDiamonds, allAchievements}) and toast/log newly unlocked skins (reuse the existing achievement popup pattern in GameCanvas/GameHUD).
9. 9. UI: extend MenuOverlay.tsx with a skin-picker row (unlockedSkins, selectedSkin, onSelectSkin props) showing locked/unlocked state; lift selectedSkin state into GameCanvas.tsx initialized from getSelectedSkin().
10. 10. In GameCanvas onSelectSkin: setSelectedSkin(id) + rendererRef.current?.setSkin(id) + threeRendererRef.current?.setSkin(id) + local state update.
11. 11. Add tests per the testStatus section; run `npm test` and `npm run build`; both must pass before commit per project CLAUDE.md.

**Open questions:**
- Should ALL skins be pre-registered into the 2D atlas (simpler, slightly larger texture) or only the selected one (requires atlas rebuild on skin change, which the current build-once SpriteCache does not support)? Recommend pre-register unlocked set at init for simplicity.
- rainbow skin: ship static base colors first, or implement the intended render-time hue cycling (needs shader uniform or pre-baked frames)? Spec comment implies cycling is desired but it is materially more work.
- Where does `allAchievements` come from for the rainbow unlock — is achievement-tracker.ts the source of truth, and what counts as 'all game achievements'? Needs a concrete predicate.
- totalDeaths/totalDiamonds have no historical data (never persisted). Acceptable to start counting from zero going forward, or should existing players be back-credited somehow? Recommend start-from-zero.
- Should the skin picker also appear on the GameHUD (mid-run) and GameOverOverlay, or menu-only? Menu-only is simplest and matches the 'select before play' model.
- Does the diamond unlock count diamond COINS (CoinType 'diamond') cumulatively across runs, distinct from the diamond SKIN? Confirm the metric maps to coin collection, not score.

## GHOST-RUN system: record best run's frames, persist to localStorage, replay + draw a translucent ghost on the next run in both 2D (WebGL) and 3D (Three.js) renderers.  (wired: not, risk: medium)

**Current state:** The ghost data layer is fully built and unit-tested but COMPLETELY UNWIRED — `GhostRecorder`/`GhostReplayer`/`saveGhostToStorage`/`loadGhostFromStorage`/`clearGhostFromStorage`/`shouldUpdateGhost` in `src/lib/game/ghost.ts` are imported only by `src/lib/game/__tests__/ghost.test.ts`. Confirmed via grep: zero references in tick.ts, use-game-engine.ts, scene, or any renderer.

Key facts that constrain integration:
- Types exist in `src/lib/game/types.ts`: `GhostFrame {tick,x,y,dir}` and `GhostRun {score, frames, recordedAt}`. There is NO ghost field on `GameState` and NO ghost field on `RenderScene`.
- The tick loop (`src/lib/game/tick.ts` `tick()`) runs a fixed-timestep accumulator at `config.fixedTimestep`. The per-step body only advances game logic when `state.phase === "playing"`. There is no per-tick counter on GameState today, but the recorder/replayer maintain their own tick counts internally.
- Player source of truth: `state.player.gridPos {x,y}` and `state.player.facing` (`src/lib/game/types.ts` Player). Score increments in `src/lib/game/player.ts` (`state.score = newScore` ~line 187, forward progress only). Death is `killPlayer()` in `src/lib/game/collision.ts` (~line 13) which sets `phase='game_over'`, computes `totalScore = state.score + state.coinBonusScore`, and fires `callbacks.onDeath(cause, totalScore)` then `onPhaseChange('game_over')`.
- Render flow (`src/hooks/use-game-engine.ts` loop ~lines 405-438): each RAF frame calls `tick(...)`, then `buildScene(gameStateRef.current, {shake})` producing a `RenderScene`, then `active.render(scene, 0)` where `active` is the Three renderer if present else the WebGL renderer. Both implement the `GameRenderer` interface (`src/lib/game/renderer/game-renderer.ts`): `render(scene, alpha)`, `resetState()`, `setStyle()`, `resize()`, `destroy()`.
- buildScene (`src/lib/game/scene/build-scene.ts`) is the ONLY GameState→RenderScene conversion. `RenderScene` (`src/lib/game/scene/types.ts`) is the single contract both renderers consume; `RenderState` in `render-pass.ts` is just `= RenderScene`.
- 2D player draw: `SpritesPass.renderPlayer()` in `src/lib/game/renderer/passes/sprites.ts` (~line 459) uses `state.player.worldPos.x` and `worldPos.y - camera.y`, draws a shadow quad + lobster sprite via `batch.draw`. Coordinate convention: screenX = worldPos.x; screenY = worldPos.y - camera.y; elevation offset 10px.
- 3D player draw: `ThreeRenderer.syncPlayer()` in `src/lib/game/renderer/three-renderer.ts` (~line 337). Conventions: `worldX = px * PX_TO_WORLD`, `worldY = -px * PX_TO_WORLD`, `PX_TO_WORLD = TILE_SIZE/CELL_SIZE = 42/32`, player centered with `+TILE_SIZE/2` on X, hop Z = `sin(hopProgress*PI)*15`, facing→rotation.z map. Pooled meshes via `createPlayer()` in `three-objects.ts`.
- The renderer split is canvas-based: pixel/voxel style toggles by switching between the WebGL2 canvas and the Three canvas (per three-renderer comment). GameCanvas.tsx owns both refs.
- Recorder only stores deltas (frame pushed when x/y/dir changes) and counts EVERY call as a tick; replayer interpolates by holding the latest frame ≤ current tick. For frames to line up between record and replay, record() and getPositionAtTick() must be called on the SAME cadence (every playing tick), starting from the same point (phase transition menu/game_over → playing).

**Integration points:**
- src/lib/game/tick.ts :: tick() — inside `if (state.phase === 'playing')` block (after updatePlayer, ~line 215), call a new module-level recorder hook to record player gridPos+facing once per playing tick; and advance a replay cursor that writes ghost pos into state. Simplest decoupling: add ghost fields to GameState and a `recordGhostTick(state)` + `advanceGhost(state)` helper rather than reaching into the React hook from the engine.
- src/lib/game/tick.ts :: resetForNewGame() (~line 252) — reset the recorder, reset/rearm the replayer from the persisted ghost, and reset state.ghostTick to 0. This is the natural 'new run starts' hook (called on game_over→playing).
- src/lib/game/tick.ts :: createInitialState() (~line 114) — initialize new ghost fields on GameState (ghostTick:0, ghostPos:null, ghostScore:0).
- src/lib/game/collision.ts :: killPlayer() (~line 28-36) — at death, call shouldUpdateGhost(totalScore, persisted) and if true build a GhostRun from the recorder frames and saveGhostToStorage(). Engine is pure TS and ghost.ts already uses localStorage with try/catch, so this is consistent. Alternatively do the save in use-game-engine onDeath callback to keep collision.ts free of persistence — see Recipe for the recommended seam.
- src/hooks/use-game-engine.ts :: setup useEffect (~line 187) — after createInitialState, loadGhostFromStorage() and attach a GhostReplayer to a ref; on onPhaseChange 'playing' (~line 248) reset recorder + rearm replayer; in onDeath (~line 266) persist via shouldUpdateGhost+saveGhostToStorage (recommended seam, keeps engine pure).
- src/lib/game/scene/build-scene.ts :: buildScene() (~line 27) — add `ghost` to the returned RenderScene (null when no ghost / beaten / not playing).
- src/lib/game/scene/types.ts :: RenderScene — add `ghost: { x:number; y:number; dir:Direction } | null` (grid coords, renderer converts to its own space).
- src/lib/game/renderer/passes/sprites.ts :: SpritesPass.renderPlayer() / new renderGhost() (~line 459) — draw a translucent lobster at ghost grid pos using the SAME worldPos math (gridX*cellSize, gridY*cellSize - camera.y), alpha ~0.35, optionally desaturated/blue tint.
- src/lib/game/renderer/three-renderer.ts :: new syncGhost() called from render() (~line 222) — a second pooled/created ghost player mesh positioned with the same PX_TO_WORLD conventions as syncPlayer, material set transparent opacity ~0.35.
- src/lib/game/renderer/three-objects.ts — add a `createGhost()` (or reuse createPlayer() and override materials to transparent) for the Three ghost mesh.

**State/type changes:**
- Add to GameState (src/lib/game/types.ts): `ghostTick: number` (counts playing ticks for replay sync), `ghostPos: { x: number; y: number; dir: Direction } | null` (current ghost grid pos for this tick, null = no ghost/beaten). Initialize in createInitialState and reset in resetForNewGame.
- Add to RenderScene (src/lib/game/scene/types.ts): `ghost: { x: number; y: number; dir: Direction } | null`.
- buildScene maps state.ghostPos → scene.ghost (or null when phase !== 'playing').
- No change to GhostFrame/GhostRun — they are already complete and tested.
- Engine-side ghost wiring (recorder instance + replayer instance) should live OUTSIDE pure GameState if you want to keep GameState serializable; recommended: hold GhostRecorder/GhostReplayer in refs in use-game-engine.ts and have the engine expose tiny hooks, OR attach them to a non-serialized engine context object. Decide one (see Open Questions).

**Balance params:**
- GHOST_STORAGE_KEY already = 'adventure_ghost_run' (ghost.ts) — reuse.
- Ghost render alpha/opacity: 0.35 default (0.25-0.5 range).
- Ghost tint: blue/cyan or desaturated to read as 'past self' (e.g. multiply palette toward #6fa8dc).
- Recording cadence: exactly once per playing tick (already implied by recorder's per-call tick increment).
- Replay end behavior: hold last frame (replayer already does this) vs hide ghost after getLastTick — default hold.
- Hide ghost once beaten: GhostReplayer.markBeaten() when state.score > ghostScore (replayer.isBeaten already provides the predicate).
- Max frames sanity cap before save (optional): skip extreme-length runs to bound localStorage; not strictly needed since deltas are small.

**Rendering needs:**
- 2D: new SpritesPass.renderGhost(state) in src/lib/game/renderer/passes/sprites.ts — draw a translucent lobster sprite (reuse `lobster_${dir}_idle` region) at gridX*cellSize / gridY*cellSize - camera.y with alpha ~0.35 and optional tint; draw BEFORE the real player so the player renders on top. Guard on state.ghost !== null and viewport cull like other entities.
- 3D: new ThreeRenderer.syncGhost(state) in src/lib/game/renderer/three-renderer.ts — maintain a single ghost mesh (createGhost()/createPlayer with transparent material opacity 0.35), position with the same PX_TO_WORLD math as syncPlayer (worldX = ghost.x*cellSize*PX_TO_WORLD + TILE_SIZE/2, worldY = -ghost.y*cellSize*PX_TO_WORLD), rotation.z from facing map, visible only when state.ghost !== null.
- Three ghost material: set `transparent:true, opacity:0.35, depthWrite:false` to avoid sorting artifacts; reuse player geometry to keep it cheap.
- No new shaders or post passes required — alpha blending is already enabled in SpritesPass.execute (gl.enable(BLEND)).

**Test status:** Excellent existing coverage for the DATA layer: src/lib/game/__tests__/ghost.test.ts thoroughly tests GhostRecorder (delta encoding, tick counting, reset, copy semantics), GhostReplayer (lookup, hold-last, beaten, reset, empty), localStorage persistence (save/load/clear/invalid), and shouldUpdateGhost. ZERO tests for the WIRING. New tests needed: (1) tick.ts records a frame per playing tick and advances state.ghostPos from a seeded replayer (engine integration test, mirror style of src/lib/game/__tests__/engine.test.ts); (2) buildScene maps state.ghostPos→scene.ghost and null when not playing (extend src/lib/game/scene/__tests__/build-scene.test.ts); (3) killPlayer/onDeath persists a GhostRun only when shouldUpdateGhost is true (mock localStorage like ghost.test.ts); (4) replayer is rearmed and recorder reset on resetForNewGame; (5) ghost cleared/hidden when beaten. Renderer draw calls are not currently unit-tested at the draw level (game-renderer.test.ts exists but is interface-level) — add a smoke assertion that renderGhost is skipped when scene.ghost is null.

**Recipe:**
1. 1. Types: add `ghostTick: number` and `ghostPos: {x:number;y:number;dir:Direction}|null` to GameState in src/lib/game/types.ts. Add `ghost: {x:number;y:number;dir:Direction}|null` to RenderScene in src/lib/game/scene/types.ts.
2. 2. Initialize the new GameState fields in createInitialState (tick.ts ~line 167 return object: ghostTick:0, ghostPos:null) and reset them in resetForNewGame (tick.ts ~line 300: state.ghostTick=0, state.ghostPos=null).
3. 3. Decouple cleanly: hold GhostRecorder + GhostReplayer as refs in src/hooks/use-game-engine.ts (recorderRef, replayerRef). Pass them to the engine via a lightweight ghost-context object OR — to keep the tick loop pure — drive ghost from the hook's loop instead of inside tick(). RECOMMENDED minimal-coupling seam: in the RAF loop (use-game-engine.ts ~line 414, right after `tick(...)`), if phase==='playing': call recorderRef.current.record(gridPos.x, gridPos.y, facing) and set gameStateRef.current.ghostPos = replayerRef.current?.getPositionAtTick(recorderRef.current.getTick()) ?? null; also call markBeaten when score>ghostScore. This keeps tick.ts 100% untouched — the recorder's own tick counter is the cadence. (Only fall back to editing tick.ts if you need deterministic engine-only tests; see Open Questions.)
4. 4. On run start: in onPhaseChange 'playing' (use-game-engine.ts ~line 248) do recorderRef.current.reset(); replayerRef.current?.reset(); set gameStateRef.current.ghostTick=0. Note: this fires both for fresh start and game_over→restart, so it is the single arm point.
5. 5. Load ghost: in the setup useEffect (use-game-engine.ts ~line 187) after createInitialState, const g = loadGhostFromStorage(); replayerRef.current = g ? new GhostReplayer(g) : null. Keep the GhostRun in a ref (currentGhostRef) for the shouldUpdateGhost comparison at death.
6. 6. Persist best: in onDeath (use-game-engine.ts ~line 266), const frames = recorderRef.current.getFrames(); if (shouldUpdateGhost(finalScore, currentGhostRef.current)) { const run = { score: finalScore, frames, recordedAt: new Date().toISOString() }; saveGhostToStorage(run); currentGhostRef.current = run; replayerRef.current = new GhostReplayer(run); }. (finalScore here is totalScore incl. coin bonus — match shouldUpdateGhost semantics; pick score vs totalScore consistently and document it.)
7. 7. Beaten handling: when gameStateRef score exceeds replayer.getScore(), call replayerRef.current.markBeaten() so getPositionAtTick returns null and the ghost disappears (already supported).
8. 8. Scene: in buildScene (build-scene.ts) add `ghost: state.phase === 'playing' ? state.ghostPos : null` to the returned object.
9. 9. 2D draw: add SpritesPass.renderGhost(state) in passes/sprites.ts; call it inside execute() just before renderPlayer (so player draws on top). Reuse the lobster idle region for scene.ghost.dir, draw at gridX*cellSize / gridY*cellSize-camera.y with alpha ~0.35 + viewport cull. Add a setGhost-style guard: if !state.ghost return.
10. 10. 3D draw: add ThreeRenderer.syncGhost(state) called from render() after syncPlayer (~line 223). Lazily create a single ghost mesh (createGhost() in three-objects.ts or createPlayer() with materials cloned to transparent opacity 0.35 depthWrite:false). Position/rotate using the same conventions as syncPlayer; mesh.visible = state.ghost !== null.
11. 11. three-objects.ts: add createGhost() (or a makeTransparent(group) helper) and ensure it is disposed in ThreeRenderer.destroy() and not leaked in resetState().
12. 12. Tests: extend build-scene.test.ts (ghost mapping), add ghost-wiring tests (record cadence, persist-only-when-better, rearm on restart, beaten hides ghost) modeled on engine.test.ts + ghost.test.ts mocks. Run `npm test` and `npm run build`; per project policy commit + push to main when green.
13. 13. Optional polish: clear ghost via clearGhostFromStorage() if you add a 'reset records' action; add a subtle label/marker so users understand the translucent lobster is their best run.

**Open questions:**
- Score semantics for the ghost: persist against `finalScore` (totalScore incl. coin bonus, what onDeath receives) or pure forward-progress `state.score`? The replayer's frames are keyed to forward-progress ticks but `isBeaten` compares a raw score — pick ONE definition and use it for both shouldUpdateGhost and markBeaten to avoid the ghost vanishing at the wrong moment.
- Coupling seam: drive ghost from the RAF loop in use-game-engine.ts (zero tick.ts edits, but recording cadence = RAF-derived tick count, not strictly engine-deterministic) vs. inside tick.ts (deterministic, engine-testable, but couples the pure engine to ghost state). Recipe recommends the hook seam; confirm whether deterministic engine-level replay tests are required.
- Tick alignment robustness: recorder counts every record() call as a tick and replayer reads getPositionAtTick(recorder.getTick()). As long as both are advanced exactly once per playing tick they align. If pause/visibility skips ticks for the live player, the ghost naturally pauses too (both gated on phase==='playing') — confirm that is the desired behavior (it is, for fairness).
- Ghost during the menu/pre-start frames: keep null until phase==='playing' (recipe does this). Confirm we do NOT want a 'demo ghost' on the menu screen.
- 3D ghost mesh lifecycle: single persistent mesh toggled visible vs pooled — single mesh is simplest; confirm no issue with it lingering across runs (just hide when scene.ghost is null).
- Storage size: extremely long runs accumulate delta frames; do we need a frame cap or compression beyond the existing delta encoding? Likely fine, but confirm acceptable localStorage footprint for high scores.
- Visual identity: tint/opacity for the ghost (blue translucent 'past self' vs plain alpha) — needs a quick design call so it reads clearly in both pixel and voxel styles.

## Frame interpolation for buttery motion in ClaudeBot's Adventure game engine. Recommended approach: camera-only interpolation first (lerp camera.y by alpha in buildScene), with optional later extension to player/obstacle interpolation.  (wired: not, risk: low)

**Current state:** The fixed-timestep accumulator and the render alpha plumbing both already exist but interpolation is unused. In src/lib/game/tick.ts, tick() accumulates deltaTime into state.timeAccumulator and drains it in a while loop at config.fixedTimestep (1/60). After the loop, state.timeAccumulator holds the leftover sub-frame remainder (0..fixedTimestep) — this is exactly the interpolation fraction, but it is currently never read by the renderer. In src/hooks/use-game-engine.ts the rAF loop (loop() around line 405-441) calls tick(), then buildScene(gameStateRef.current, {shake}), then active.render(scene, 0) — alpha is HARDCODED to 0 (line 437). Both renderers accept render(scene, alpha) per the GameRenderer interface (src/lib/game/renderer/game-renderer.ts line 11) but both IGNORE it: renderer.ts line 167 `render(scene, _alpha)` and three-renderer.ts line 197 `render(scene, _alpha: number)`. So the contract for interpolation is present but inert. The judder source: camera moves only on fixed ticks via updateCamera() (src/lib/game/camera.ts) `camera.y += (camera.targetY - camera.y) * config.cameraSmoothing (0.1)`. On high-refresh displays (120/144Hz) or whenever rAF outpaces the 60Hz sim, multiple rAF frames render the SAME camera.y between ticks, producing stair-step / judder, especially while the camera is smoothly chasing the player after a hop. Everything in both renderers is drawn relative to camera.y: 2D sprites.ts computes `screenY = worldY - camera.y` (lines 103, 378, 430, 470, 481); three-renderer.ts derives camera world position from `cameraY = -state.camera.y * PX_TO_WORLD` (line 212). The player itself already moves smoothly WITHIN a tick because updatePlayer() lerps worldPos by hopProgress each fixed step and hopDuration is only 0.12s (~7 ticks) — so per-frame camera interpolation alone removes the dominant judder. NOTE: buildScene currently passes scene.camera = state.camera BY REFERENCE (build-scene.ts line 35), and build-scene.test.ts line 16 asserts `expect(scene.camera).toBe(state.camera)` (reference identity). Interpolation requires returning a NEW camera object, which breaks that assertion.

**Integration points:**
- src/hooks/use-game-engine.ts: the rAF loop() ~line 431-437 — compute alpha = state.timeAccumulator / DEFAULT_CONFIG.fixedTimestep (clamp 0..1), pass it into buildScene as opts.alpha AND into active.render(scene, alpha). Currently buildScene is called with only {shake} and render with hardcoded 0.
- src/lib/game/scene/build-scene.ts: buildScene() — add alpha to BuildSceneOptions, compute an interpolated camera.y from a stored previous camera.y and the current camera.y, and return a NEW camera object {y: interpolatedY, targetY, viewportWidth, viewportHeight} instead of the live state.camera reference.
- src/lib/game/camera.ts: updateCamera() — must record the pre-update camera.y into a new GameState field (e.g. state.camera.prevY = state.camera.y) at the START of updateCamera so buildScene can lerp between prevY and y. This is the only correct place to snapshot, since it runs once per fixed tick.
- src/lib/game/tick.ts: createInitialState() and resetForNewGame() — initialize/reset the new prevY field so a new game does not interpolate from a stale previous camera position (camera reset discontinuity).
- src/lib/game/scene/__tests__/build-scene.test.ts line 16: change `expect(scene.camera).toBe(state.camera)` from reference identity to value equality (toEqual) since buildScene now returns a fresh camera object.
- src/lib/game/renderer/renderer.ts render() and src/lib/game/renderer/three-renderer.ts render(): no change needed if interpolation is folded into scene.camera.y inside buildScene (renderers already key everything off scene.camera.y). Alternatively the alpha param could be consumed directly inside each renderer, but folding into buildScene keeps both renderers identical and is the safest single-source approach.

**State/type changes:**
- Add a field to the Camera type (src/lib/game/types.ts) to hold the previous fixed-tick camera.y: e.g. `prevY: number`. Update createInitialState() (tick.ts ~line 160) and resetForNewGame() (tick.ts ~line 296) to set prevY = y.
- Add `alpha?: number` to BuildSceneOptions in src/lib/game/scene/build-scene.ts (default 0 to preserve current snap behavior when omitted, e.g. in tests).
- No change to the RenderScene type is required (scene.camera stays a Camera); but the camera object returned by buildScene becomes a NEW object rather than the live reference. If prevY is added to Camera, scene.camera will carry it harmlessly.
- Optional alternative to mutating the Camera type: store prevCameraY on GameState root instead of inside camera, to avoid touching the Camera shape consumed by renderers. Either works; prevY-on-camera keeps it co-located.

**Balance params:**
- alpha = clamp(state.timeAccumulator / config.fixedTimestep, 0, 1) — the interpolation fraction; 0 = just ticked, ~1 = about to tick.
- config.fixedTimestep = 1/60 (existing, constants.ts line 11) — denominator for alpha.
- config.cameraSmoothing = 0.1 (existing, camera.ts) — unchanged; interpolation sits on TOP of smoothing.
- Interpolation should be RENDER-ONLY: never write the interpolated value back into state.camera.y. Only ever lerp(prevY, y, alpha) into the throwaway scene camera.
- Optional feature flag / config bool to disable interpolation (e.g. for reduced-motion or debugging) defaulting to enabled.

**Rendering needs:**
- No new draw calls. The only rendering-facing change is that scene.camera.y becomes the interpolated value (prevY lerped toward y by alpha) instead of the raw fixed-tick value. Both renderers already consume scene.camera.y for all positioning, so they automatically become smooth.
- Three-renderer cull (isLaneVisible, line 297) uses camera.y too — it will receive the interpolated y, which is fine (slightly smoother culling boundary, no correctness issue).
- Particles in 2D are drawn with scene.camera.y (renderParticles, renderer.ts line 179) — they will also interpolate consistently with everything else, avoiding particle-vs-world tearing.

**Test status:** Existing coverage: src/lib/game/scene/__tests__/build-scene.test.ts covers buildScene field copying, deathProgress, and shake defaults, and currently asserts scene.camera===state.camera (reference) — this MUST change to value equality (toEqual) because buildScene will return a fresh camera. No existing test covers camera interpolation. New tests needed: (1) buildScene with alpha=0 returns camera.y === state.camera.y (snap, back-compat); (2) buildScene with alpha=0.5 returns camera.y === lerp(prevY, y, 0.5); (3) buildScene with alpha omitted defaults to no interpolation / current camera.y; (4) updateCamera() records prevY = old camera.y before advancing; (5) resetForNewGame() resets prevY to camera.y so the first frame after reset does not interpolate from a stale position (camera reset / no rubber-band). Run npm test + npm run build per project workflow.

**Recipe:**
1. 1. Add `prevY: number` to the Camera type in src/lib/game/types.ts.
2. 2. In tick.ts createInitialState(), set camera.prevY = camera.y at creation (~line 160-165). In resetForNewGame(), after resetting state.camera.y and targetY (~line 296-298), set state.camera.prevY = state.camera.y. This kills the new-game camera-reset rubber-band.
3. 3. In src/lib/game/camera.ts updateCamera(): at the very TOP of the function, snapshot `camera.prevY = camera.y;` BEFORE the smoothing line advances camera.y. This captures the start-of-tick position for the just-completed-to-current step.
4. 4. In src/lib/game/scene/build-scene.ts: add `alpha?: number` to BuildSceneOptions. Compute `const a = Math.min(1, Math.max(0, opts.alpha ?? 0));` and `const camY = state.camera.prevY + (state.camera.y - state.camera.prevY) * a;`. Return a NEW camera object: `camera: { y: camY, targetY: state.camera.targetY, viewportWidth: state.camera.viewportWidth, viewportHeight: state.camera.viewportHeight }` (omit/forward prevY as desired). Leave all other scene fields by reference as today.
5. 5. In src/hooks/use-game-engine.ts loop() (~line 431): after tick(), compute `const alpha = Math.min(1, gameStateRef.current.timeAccumulator / DEFAULT_CONFIG.fixedTimestep);` then `buildScene(gameStateRef.current, { shake: {...}, alpha })` and `active?.render(scene, alpha);` (replace hardcoded 0).
6. 6. Update build-scene.test.ts line 16 from `expect(scene.camera).toBe(state.camera)` to `expect(scene.camera).toEqual({ y: state.camera.y, targetY: state.camera.targetY, viewportWidth: state.camera.viewportWidth, viewportHeight: state.camera.viewportHeight })` (alpha defaults to 0 so y is unchanged; account for prevY if forwarded).
7. 7. Add new tests: alpha=0 snap, alpha=0.5 lerp, alpha omitted, updateCamera prevY snapshot, resetForNewGame prevY reset.
8. 8. Run npm test and npm run build; verify both pass. Commit + push to main.
9. 9. (Deferred, optional Phase 2) If sub-frame judder on obstacles is still visible at very high refresh, extend buildScene to interpolate obstacle.worldX and player.worldPos by alpha using per-entity prev positions — but guard ALL discontinuities first (see open questions): hop landing snap (don't interpolate the tick where hopProgress crossed 1 to a snapped gridPos), log-id change/teleport, lane pruning removing the prev entity. Camera-only (steps 1-8) avoids ALL of these and is the safe shippable win.

**Open questions:**
- Phase 2 only: hop landing snap — updatePlayer() snaps worldPos to gridPos*cellSize on the landing tick (player.ts lines 172-175). Interpolating player worldPos across that tick would visibly snap/jitter. Mitigation: skip player interpolation on any tick where a landing occurred (track a per-frame flag), or interpolate only when player.hopTarget!==null using hopProgress which is already smooth.
- Phase 2 only: log-id changes / log riding — when ridingLogId changes or the player teleports onto a log, prev worldPos is from a different lane/log; interpolating produces a slide. Mitigation: reset the player prev position on ridingLogId transitions.
- Phase 2 only: lane pruning — pruneLanesBehindPlayer removes lanes/obstacles each tick; an interpolated entity could reference a prev position for an obstacle no longer present, or a newly-generated obstacle has no prev. Mitigation: only interpolate entities present in BOTH prev and current frames (match by id), snap (alpha=1) for new ids.
- Confirm desired clamp behavior when document was hidden: cappedDt = min(dt,0.1) (use-game-engine.ts line 409) already caps the catch-up; after a long pause timeAccumulator stays < fixedTimestep so alpha stays valid — verify no spike on tab refocus.
- Decide where prevY lives: on Camera type vs on GameState root. On-camera is co-located but slightly widens the Camera shape that renderers see (harmless). Confirm preference with maintainer.

## Adventure leaderboard scoring correctness (coin double-counting + combo reward question)  (wired: partially, risk: medium)

**Current state:** There is a confirmed coin double-count in what gets stored to the DB. Data flow: (1) src/lib/game/player.ts:184-197 sets state.score = startY - gridPos.y, i.e. pure forward distance, monotonic max. (2) src/lib/game/coins.ts:200-208 accumulates state.coinBonusScore += COIN_VALUES[type] and state.coinsCollected++ on pickup. (3) src/lib/game/collision.ts:28-37 killPlayer computes totalScore = state.score + state.coinBonusScore (distance + coins), writes it to state.highScore, and passes it as the finalScore arg to callbacks.onDeath. (4) src/hooks/use-game-engine.ts:318-324 onDeath calls submitScore(finalScore, cause, "adventure", gs.coinsCollected, gs.coinBonusScore) — so the COMBINED total (distance+coins) is passed as the `score` argument, while coinBonusScore is ALSO passed separately as the `coinBonus` argument. (5) src/actions/game-scores.ts:29-37 submitScore inserts score:<combined total> into the `score` column AND coin_bonus:<coinBonusScore> into the `coin_bonus` column. So the coin value lives in BOTH the score column (folded into the combined total) and the coin_bonus column. The leaderboard (game-scores.ts:43-81, ORDER BY score DESC) therefore ranks by distance+coins — internally consistent for ranking, but the per-row coin_bonus is redundant with what is already inside score. The real corruption surfaces in getPlayerStats (game-scores.ts:95-102): totalDistance = sum(score) is labeled "distance" but actually equals sum(distance+coins) — coins counted once there; and bestCoinBonus/totalCoins re-expose coins again, so any UI that adds totalDistance + coin metrics double-counts coins. effects.ts updateCombo (lines 129-138) caps a combo count 1..8 but it is purely cosmetic: combo never multiplies score anywhere (grep confirms combo only feeds setCombo UI state and the run-summary "highestCombo" PB; no score path reads it). GameOverOverlay.tsx:97-111 shows SCORE: score and separately COINS: n (+bonus) — if score already includes the bonus, the breakdown visually implies coins are additive on top, misleading the player. DB schema: migration 008 score INT NOT NULL; migration 012 added coins_collected INT and coin_bonus INT (both default 0). Existing rows already contain the combined value in `score`.

**Integration points:**
- src/lib/game/collision.ts:killPlayer (lines 28-36) — STOP folding coins into the leaderboard number; decide totalScore semantic here
- src/hooks/use-game-engine.ts:onDeath (lines 318-324) — submitScore call: first arg currently the combined total; align it with chosen semantic
- src/actions/game-scores.ts:submitScore (lines 7-41) — `score` column write; document/enforce that `score` = distance only (raw) and coins live only in coin_bonus
- src/actions/game-scores.ts:getPlayerStats (lines 95-102) — totalDistance must sum distance only; if `score` becomes distance-only this is automatically correct, otherwise subtract coin_bonus
- src/actions/game-scores.ts:getLeaderboard (lines 50-55) — ORDER BY score; ranking semantic follows the `score` column definition
- src/components/adventure/GameOverOverlay.tsx:97-111 — SCORE vs COINS display must match chosen semantic so the breakdown is not misleading
- src/lib/game/effects.ts:updateCombo (lines 129-138) — only touch if combo is to start rewarding score

**State/type changes:**
- No type changes required for the recommended fix. GameState already separates score and coinBonusScore (src/lib/game/types.ts). The fix is purely about which number is sent to submitScore and stored.
- Optional (if you want highScore to remain a 'combined' display value while leaderboard stores raw): keep state.highScore = distance+coins for the local BEST display but send distance-only to submitScore. This is a behavioral split, not a type change.
- No DB migration is strictly required; coin_bonus and coins_collected columns already exist and are sufficient.

**Balance params:**
- COIN_VALUES (src/lib/game/constants.ts) — coin point values; unchanged by the fix but they are what defines coinBonusScore magnitude
- If combo is made to reward score: combo cap is currently Math.min(count+1, 8) and windowSec 0.8 in effects.ts — a sensible multiplier curve would be e.g. floor((combo-1)/2) bonus points per forward step, or 1 + 0.25*(combo-1) capped at 2x; default recommendation: leave combo cosmetic (no score reward) to avoid changing leaderboard balance

**Rendering needs:**
- GameOverOverlay.tsx: relabel/keep SCORE as raw distance and COINS: n (+bonus) as a separate stat, OR add an explicit TOTAL line = score+bonus, so the player understands the leaderboard ranks on distance and coins are a separate stat (whichever semantic is chosen, the three numbers must reconcile)
- CurrentRunPanel.tsx:49-101 shows live score (distance) and coins separately — already correct for raw-distance semantic; no change needed if score stays distance
- run-summary.ts already renders score and coinBonus as separate fields (lines 88-110) — consistent with raw-distance semantic; no change

**Test status:** Existing: src/actions/__tests__/game-scores.test.ts covers submitScore insert/auth/error and getLeaderboard, but does NOT assert WHICH value goes into the `score` column vs coin_bonus (it passes 100,5,10 and only checks insert was called) — so the double-count is untested. src/lib/game/__tests__/coins.test.ts asserts state.coinBonusScore accumulates (gold=5) but not the killPlayer/submit path. No test exists for collision.ts killPlayer totalScore composition, and there is no src/lib/game/__tests__/collision.test.ts. Needed new tests: (1) killPlayer sets finalScore = distance only (or chosen semantic) and does not fold coins into the submitted score; (2) submitScore is called with score=distance and coinBonus=coinBonusScore separately (assert exact args, not just that it was called); (3) getPlayerStats totalDistance equals sum of distances (not distance+coins) given mixed rows; (4) regression test asserting score column and coin_bonus column never both contain the coin value.

**Recipe:**
1. RECOMMENDED SEMANTIC: leaderboard `score` column = RAW DISTANCE only. Coins are a separate, already-stored stat (coin_bonus / coins_collected). This is the least surprising, makes 'distance' stats honest, removes the double-count, and requires the smallest code change. Combo stays COSMETIC (no score reward) to avoid silently changing competitive balance.
2. Step 1 — collision.ts killPlayer (lines 28-36): change the value passed to onDeath and stored as highScore. Replace `const totalScore = state.score + state.coinBonusScore;` Decide: if you want local BEST display to still reflect coins, keep highScore combined but pass state.score (distance) to onDeath. Simplest consistent option: set `const finalScore = state.score;` use finalScore for both the highScore comparison and callbacks.onDeath(cause, finalScore). This makes highScore = best distance, matching the leaderboard.
3. Step 2 — use-game-engine.ts onDeath (lines 318-324): submitScore(finalScore, cause, 'adventure', gs.coinsCollected, gs.coinBonusScore) now sends distance as score and coins as coin_bonus — already the right shape once finalScore is distance. No change needed beyond confirming finalScore is distance. Verify isNewHighScore comparison at lines 283-293 still compares distance vs prior distance (it will, since localStorage now stores distance).
4. Step 3 — game-scores.ts: no logic change to submitScore needed (it already writes score and coin_bonus to separate columns). getPlayerStats totalDistance = sum(score) becomes correct automatically once score = distance. Add a comment documenting `score` = raw distance, coins tracked separately.
5. Step 4 — GameOverOverlay.tsx (lines 97-111): SCORE: score is now distance (correct). Keep COINS: n (+bonus) as a separate stat. Optionally add a TOTAL: score+coinBonus line if you want to surface a combined number to the player — but it must NOT be what is stored/ranked.
6. Step 5 — Leave effects.ts updateCombo unchanged (combo remains cosmetic). Do NOT add a score multiplier unless product explicitly wants it; doing so would change leaderboard balance and require re-baselining scores.
7. Step 6 — Tests: add src/lib/game/__tests__/collision.test.ts asserting killPlayer passes distance-only as finalScore and does not include coinBonusScore; strengthen game-scores.test.ts to assert submitScore inserts score=distance and coin_bonus=coins separately; add a getPlayerStats test with mixed rows proving totalDistance excludes coins. Run npm test + npm run build.
8. Step 7 — FLAG existing DB rows: any rows written before this fix have score = distance+coins. After the fix, new rows have score = distance. This makes the leaderboard inconsistent across the migration boundary (old rows rank artificially high). See open questions for the data-migration decision.

**Open questions:**
- DB DATA SEMANTIC FLAG: existing game_scores rows store score = distance + coins, but every row also has coin_bonus populated, so historical raw distance is recoverable as score - coin_bonus. Do you want a one-time backfill UPDATE game_scores SET score = score - coin_bonus WHERE game_type='adventure' (and coin_bonus > 0) so all rows use the new raw-distance semantic? Without it the leaderboard mixes two semantics and old runs rank too high. Note rows where coin_bonus was 0 or pre-coins (migration 012) are already raw and must be excluded to avoid no-op/over-correction. This is a destructive, irreversible-in-place change to stored competitive data — recommend doing it in a migration with a backup/verification query first.
- Product decision: should the public leaderboard rank by raw distance (recommended, skill-based, coins are flavor) or by distance+coins (rewards exploration/risk)? The recommendation assumes raw distance; if distance+coins is desired instead, the minimal change flips to: keep score = combined in submitScore BUT make getPlayerStats.totalDistance subtract coin_bonus, and the double-count in the stats panel is the only bug to fix (no killPlayer change). Confirm which.
- Should combo ever reward score? Currently cosmetic. If yes, it changes leaderboard balance and would need its own design (multiplier curve, cap) plus a score-baseline reset; recommend keeping cosmetic for now.
- isNewHighScore / localStorage adventure_high_score currently persists the combined highScore; after the fix it will persist distance. Confirm that resetting/letting players' local BEST shift to distance-only is acceptable (it will appear lower than before for players who collected coins).

## Easter-egg and progression system completeness audit  (wired: partially, risk: medium)

**Current state:** EASTER EGGS (6 in src/lib/easter-eggs/registry.ts): all 6 have a discover() call wired, but only 5 are reachable.
1. konami_code -> WIRED & reachable. useKonami() (src/hooks/use-konami.ts:14) calls discover("konami_code"); mounted globally via KonamiEffects in src/app/layout.tsx:63 (dynamic import).
2. hidden_terminal ("Red Pill") -> DEAD. src/components/easter-eggs/hidden-terminal.tsx calls discover("hidden_terminal") in a useEffect when open=true, BUT <HiddenTerminal> is never rendered/mounted anywhere (grep for "<HiddenTerminal" / "HiddenTerminal(" finds only its own definition). There is no trigger to set open=true. The "red_pill" achievement and the open_hidden_terminal XP action (xp.ts:16) are therefore unreachable.
3. vault_complete -> WIRED. src/components/vault/simon-says.tsx:118 calls discover("vault_complete") on puzzle completion.
4. cosmonaut_cocktail -> WIRED. src/components/demos/cocktail-mixer/index.tsx:59 calls discover() when a secret drink is made. NOTE: this egg has NO achievementId in the registry, so no achievement fires (intentional? the other 4 non-vaporwave eggs reference achievements).
5. hitchhiker_42 -> WIRED. src/components/adventure/GameCanvas.tsx:106 fires discover("hitchhiker_42") when score===42. Also has no achievementId.
6. vaporwave -> WIRED. src/lib/terminal/commands.ts:216 calls ctx.onDiscover?.("vaporwave"); onDiscover is plumbed from useTerminal (src/hooks/use-terminal.ts:73). No achievementId.

discover() (src/hooks/use-easter-egg.ts:11) does: awardXP("find_easter_egg",{key:eggId}), trackEvent("find_easter_egg"), unlockAchievement(egg.achievementId) if present, addDiscovery(eggId). All gated on isAuthenticated in visitor-context (awardXP/unlockAchievement return early if !isAuthenticated), so anonymous visitors get NOTHING from eggs.

ACHIEVEMENTS (28 total; 13 site + 15 game in src/lib/achievements.ts): conditions are declarative ({type:"event"|"event_count"|"score"|"streak"|"manual"}) but THERE IS NO RUNTIME EVALUATOR for them. The only place conditions are read is src/lib/game/skins.ts (unrelated skin-unlock system). Achievements only fire when something imperatively calls unlockAchievement(id). Consequences:
- DEAD achievements (condition defined, never unlocked anywhere): first_steps (event first_visit - first_visit XP/event never fired anywhere), road_scholar (event_count view_project x3 - view_project never awarded/tracked from any page; grep finds no awardXP("view_project") in src/app), pixel_perfect (event fill_canvas - never fired), night_owl (event toggle_theme - navbar.tsx:33 awards XP toggle_theme but NEVER calls unlockAchievement("night_owl")), cartographer (event_count find_easter_egg x5 - events ARE tracked by discover(), but nothing counts them to unlock; also only 5 eggs are discoverable and one is dead so max reachable is 5 events, exactly the threshold, but no evaluator reads it), red_pill (manual, but trigger is dead - see egg #2), streak_3/streak_7 (unlocked imperatively in visitor-context.tsx:105-109, so these DO fire).
- WORKING achievements: mixologist (manual count in cocktail-mixer/index.tsx:54), konami/halliday_egg (via egg achievementId), streak_3/streak_7, all 15 game achievements + hop_skip/road_warrior (unlocked imperatively from GameCanvas/game tracker).

XP ACTIONS (14 in src/lib/xp.ts): never-fired actions = first_visit, view_project (no caller in app pages), export_pixel_art, open_hidden_terminal (dead, tied to unmounted HiddenTerminal). Fired actions: toggle_theme (navbar), use_demo (cocktail), play_game/score_50/score_100/score_200 (GameCanvas), find_easter_egg (discover), open_terminal (terminal-toggle.tsx:18), streak_3/streak_7 (achievement reward path).

**Integration points:**
- src/components/easter-eggs/hidden-terminal.tsx -> currently orphaned; must be mounted in a layout/page (e.g. src/app/(public)/layout.tsx or home page) with an open-state trigger to make the Red Pill egg reachable
- src/app/layout.tsx:63 (KonamiEffects mount) -> reference pattern for how to mount a global egg component
- src/app/(public)/work/* and project detail pages -> add awardXP("view_project",{key:slug}) + trackEvent to feed road_scholar
- src/components/layout/navbar.tsx:33 -> after awardXP("toggle_theme") also call unlockAchievement("night_owl")
- src/lib/visitor-context.tsx awardXP/loadProfile -> add a fire of first_visit (XP + first_steps achievement) on initial profile load
- new: src/lib/achievements.ts -> add an evaluateAchievements(profile,event) condition engine, OR keep imperative model and add explicit unlockAchievement calls for road_scholar/pixel_perfect/cartographer/first_steps
- pixel-art demo component (src/components/demos/.../pixel*) -> fire fill_canvas event + export_pixel_art XP + pixel_perfect achievement
- src/hooks/use-easter-egg.ts:24 -> after the 5th distinct discovery, call unlockAchievement("cartographer") (count distinct profile.discoveries)

**State/type changes:**
- No type changes strictly required for the imperative fix path.
- If building a condition evaluator: add a function signature in src/lib/achievements.ts like evaluateAchievements(ctx:{achievements:string[];discoveries:string[];events:Record<string,number>;streakDays:number;scores:Record<string,number>}):string[] returning newly-satisfied IDs; this consumes the existing AchievementCondition union in src/lib/types.ts:88-92 (event/event_count/score/streak/manual) with no schema change.
- Optionally add achievementId for cosmonaut_cocktail/hitchhiker_42/vaporwave in registry.ts if those eggs should grant dedicated achievements (currently they only grant generic find_easter_egg XP).

**Balance params:**
- cartographer threshold currently 5 (achievements.ts:115) == number of reachable eggs once Red Pill is fixed (6 eggs but only 5 fire find_easter_egg if cosmonaut counts... actually all 6 call discover so all 6 fire find_easter_egg) -> reconcile threshold vs egg count (set count to 6 or keep 5)
- find_easter_egg XP = 50, rule once_ever but dedup key is per-egg (meta.key) so each egg awards 50 -> intentional, verify desired
- first_visit XP = 10 (once_ever)
- open_hidden_terminal XP = 25 (once_ever) - only meaningful once egg #2 is reachable
- view_project XP = 5 (per_session), road_scholar needs 3 distinct projects

**Rendering needs:**
- Red Pill: need a UI affordance/trigger that opens <HiddenTerminal> (e.g. a hidden clickable hotspot on the home hero, or a key combo) and the component must be mounted; today nothing renders it.
- No canvas/3D rendering changes needed for the progression fixes.

**Test status:** Existing tests: src/lib/__tests__/unified-achievements.test.ts (data-integrity only: counts, IDs, icons, contexts - does NOT test that conditions ever fire), src/lib/__tests__/achievements.test.ts, src/lib/terminal/__tests__/commands.test.ts:83 (vaporwave triggers onDiscover), src/components/demos/cocktail-mixer/__tests__ (mocks useEasterEgg). There are NO tests asserting an egg is actually mounted/reachable, and NO tests that achievement conditions auto-evaluate. New tests needed: (1) a render test mounting HiddenTerminal and asserting discover fires; (2) integration test that discover() unlocks linked achievement + addDiscovery; (3) if a condition evaluator is added, unit tests per condition type; (4) test that navbar toggle unlocks night_owl; (5) test cartographer unlocks after N discoveries.

**Recipe:**
1. 1. Decide model: keep imperative unlockAchievement() calls (lower risk, matches existing code) OR add a central evaluateAchievements() engine. Recommend imperative for the dead-but-trigger-exists cases and a small counter for cartographer.
2. 2. Fix Red Pill (egg #2): mount <HiddenTerminal open onClose> in a client component and add a trigger (hidden hotspot or key combo) on the home page; wire open state. This activates hidden_terminal discovery, red_pill achievement, and open_hidden_terminal XP. Add awardXP("open_hidden_terminal") at the open trigger.
3. 3. Fire first_visit: in src/lib/visitor-context.tsx after first successful profile load, call awardXP("first_visit") and unlockAchievement("first_steps") (guard once_ever via existing dedup).
4. 4. Wire view_project + road_scholar: on each project/work detail page add awardXP("view_project",{key:slug}) and trackEvent; after 3 distinct, unlockAchievement("road_scholar") (count distinct from profile events or a local set).
5. 5. Wire night_owl: in navbar.tsx after awardXP("toggle_theme") add unlockAchievement("night_owl").
6. 6. Wire cartographer: in use-easter-egg.ts discover(), after addDiscovery, if distinct discoveries length >= threshold call unlockAchievement("cartographer"). Reconcile threshold (5) with actual egg count (6) in achievements.ts:115.
7. 7. Wire pixel demo: locate the pixel-art demo, fire fill_canvas event + unlockAchievement("pixel_perfect") on full canvas and awardXP("export_pixel_art") on export.
8. 8. (Optional) add achievementId entries for cosmonaut_cocktail/hitchhiker_42/vaporwave if dedicated achievements are desired.
9. 9. Add tests for each newly-wired path (render HiddenTerminal, discover->achievement, navbar->night_owl, cartographer threshold).
10. 10. Run npm test && npm run build; commit to main per project workflow.

**Open questions:**
- Are cosmonaut_cocktail, hitchhiker_42, and vaporwave INTENTIONALLY without an achievementId (generic XP only), or should they each grant a unique achievement?
- Should easter eggs / XP work for anonymous (unauthenticated) visitors? Currently visitor-context.awardXP and unlockAchievement no-op when !isAuthenticated, so all eggs silently do nothing for logged-out users.
- Is the absence of an achievement-condition evaluator intentional (imperative-only design) or was an evaluator planned (conditions look declarative but are unused outside game skins)?
- cartographer count is 5 but there are 6 eggs all calling find_easter_egg - which is the intended target?
- Where should the Red Pill trigger live, and what is the intended discovery mechanic (key combo vs hidden click hotspot)?
- first_visit/first_steps: should these fire for any profile load, or strictly the very first ever visit (needs server-side once_ever guard, not just session dedup)?

## Analysis of 61 GitHub Dependabot vulnerabilities on portfolio-tool-hub (local npm audit reports 16: 1 critical, 10 high, 5 moderate). Dependabot's higher count reflects per-advisory counting and stale lockfile snapshots; local audit is the authoritative current state.  (wired: n/a, risk: low)

**Current state:** package.json (C:\Users\Travesty\Desktop\Projects\portfolio-tool-hub\package.json) pins next exactly at "15.5.12" (no caret) and vitest at "^4.0.18". Local `npm audit --json` = 16 vulns (1 critical, 10 high, 5 moderate). Crucially, `npm audit --omit=dev` (production-only) = ONLY 3 vulns: next (high), postcss (moderate), ws (moderate). All 13 other advisories are dev/build-time only. Dependency tree origins: vitest@4.0.18 (direct dev, CRITICAL GHSA-5xrq-8626-4rwp CVSS 9.8 UI-server arbitrary file read/exec, <4.1.0); vite@7.3.1+esbuild@0.27.3 via @vitejs/plugin-react and vitest (dev); jsdom@28.1.0->undici@7.22.0 (dev test env); shadcn@3.8.5->@modelcontextprotocol/sdk@1.26.0->@hono/node-server, express-rate-limit, hono, fast-uri (dev CLI, runs only when scaffolding components); ws@8.19.0 via @supabase/supabase-js->@supabase/realtime-js (PROD but moderate uninitialized-memory disclosure requiring PR:H); postcss via next (PROD moderate); brace-expansion/picomatch/flatted/path-to-regexp/qs/ip-address are deep transitive (mostly dev toolchain). Genuinely production-reachable: next (the only high-severity prod-exploitable one) and ws (low practical risk).

**Integration points:**
- C:\Users\Travesty\Desktop\Projects\portfolio-tool-hub\package.json: dependencies.next "15.5.12" -> bump to "15.5.19" (or latest 15.5.x patch) to clear the 6 Next.js advisories (SSRF on WebSocket upgrade GHSA-c4j6-fc7j-m34r, RSC cache poisoning GHSA-wfc6-r584-vfw7/GHSA-vfv6-92ff-j949, CSP-nonce XSS GHSA-ffhc-5mcf-pf4q, two DoS GHSA-8h8q-6873-q5fj/GHSA-mg66-mrh9-m8jx)
- C:\Users\Travesty\Desktop\Projects\portfolio-tool-hub\package.json: devDependencies.vitest "^4.0.18" already caret-ranged -> plain `npm audit fix` lifts it to 4.1.8, clearing the CRITICAL
- C:\Users\Travesty\Desktop\Projects\portfolio-tool-hub\package.json-lock (package-lock.json): plain `npm audit fix` cleanly bumps vite 7.3.1->7.3.5, undici 7.22->7.27.1, ws 8.19->8.21, postcss 8.5.6->8.5.15, hono 4.12.3->4.12.23, @hono/node-server 1.19.9->1.19.14, express-rate-limit 8.2.1->8.5.2 (all non-major)
- Netlify build (Node 20, travisjohnjones.com): only the next bump and postcss/ws prod bumps affect the deployed artifact; verify with `npm run build`
- Test runner config (vitest) used by `npm test` per CLAUDE.md workflow: validate vitest 4.1.8 still runs the suite after fix

**State/type changes:**
- No source/type changes required. This is a dependency-version remediation only.
- package.json: next "15.5.12" -> "15.5.19" (manual edit, because the exact pin prevents `npm audit fix` from touching it)
- package-lock.json regenerated by `npm audit fix` for the dev/transitive bumps

**Balance params:**
- next: stay on 15.5.x (15.5.19) — do NOT jump to 16.x (major, breaking). Audit range tops out at 16.3.0-canary so 15.5.19 satisfies the fix without a major
- vitest: 4.1.8 (same major v4, safe)
- Avoid `npm audit fix --force` — it is the only path that would pull a Next major and risk shadcn/MCP-SDK chain churn for the remaining dev-only fast-uri/flatted/path-to-regexp/hono advisories
- shadcn dev-CLI advisories: acceptable to defer; only execute when scaffolding components, never in prod runtime

**Rendering needs:**

**Test status:** Per CLAUDE.md the workflow requires `npm test` (vitest) + `npm run build` to pass before commit. After remediation: run `npm test` to confirm vitest 4.1.8 runs the existing suite unchanged, and `npm run build` to confirm next 15.5.19 + postcss bump build the Netlify artifact. No new tests are needed (dependency bump, no behavior change); a smoke check that GameCanvas/embed routes render is sufficient. No existing test targets the vulnerable code paths.

**Recipe:**
1. 1. Snapshot baseline: `npm audit --json` and `npm audit --omit=dev --json` (already done: 16 total, 3 prod) so you can diff after.
2. 2. Run plain `npm audit fix` (NO --force). This non-breakingly clears the CRITICAL vitest (->4.1.8) plus vite, undici, ws, postcss, hono, @hono/node-server, express-rate-limit — all same-major patch bumps.
3. 3. Manually bump the one prod high that audit-fix skips due to the exact pin: edit package.json dependencies.next from "15.5.12" to "15.5.19" (latest 15.5.x patch), then `npm install`. This is the single most important fix — it is the only high-severity, production-reachable advisory cluster (SSRF, RSC cache poisoning, CSP XSS, DoS).
4. 4. Re-run `npm audit --omit=dev` to confirm prod vulns drop from 3 to ~0.
5. 5. Run `npm test` (vitest 4.1.8) — confirm suite passes.
6. 6. Run `npm run build` — confirm Next 15.5.19 production build succeeds for Netlify.
7. 7. Re-run full `npm audit`; expect remaining items to be dev-only deep-transitive (fast-uri, flatted, path-to-regexp, brace-expansion, picomatch, qs, ip-address) reachable only via the shadcn CLI / MCP SDK at scaffold time — document as accepted risk rather than forcing a breaking shadcn major.
8. 8. (Optional, separate change-set) if you want those dev advisories gone, bump shadcn to its latest, re-audit, and re-test in isolation — do NOT bundle with step 2-3.
9. 9. Commit and push to main per CLAUDE.md workflow once test+build are green.

**Open questions:**
- Dependabot reports 61 advisories vs 16 from local audit — is the GitHub default-branch package-lock.json older/diverged from the working tree? Reconcile by ensuring the committed lockfile matches the post-fix state.
- Does shadcn need to remain a devDependency at all post-scaffolding? Removing it would eliminate the largest cluster (hono/MCP-SDK/fast-uri/express-rate-limit/path-to-regexp) of dev advisories outright.
- Is the next bump 15.5.19 confirmed published, or should it be the newest 15.5.x at fix time? Verify against npm registry before pinning.
- ws@8.21 reaches prod via @supabase/realtime-js — confirm Supabase realtime is actually used at runtime; if not, the ws moderate is effectively non-reachable.

