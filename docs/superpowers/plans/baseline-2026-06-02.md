# Stabilization baseline — 2026-06-02

Captured before the renderer refactor (Spec #1). This is the "before" snapshot the Phase 3
QA sweep must match or beat (success criterion: zero failures, no regressions).

## npm test

**PASS** — 48 test files, **668 tests passed**, 0 failed. (~6.4s)

Non-failing noise (expected, asserted by tests):
- `voxel-loader.test.ts` logs `Failed to load voxel sprite v2_fail: 404` — intentional (tests the skip-on-404 path).
- `use-three-renderer.test.ts` logs `canvas ref not ready on activation` — intentional (tests the deactivation/null path).

## npm run build

**PASS** — production build succeeds. `/adventure` route compiles (173 kB / 346 kB First Load JS).
**The `_document.js` / `[turbopack]_runtime.js` error does NOT reproduce in production build.**
It is a `next dev --turbopack` runtime error observed when toggling 3D/voxel mode — a dev-mode
issue, not a build blocker. See `3d-ssr-diagnosis.md`.

## npm run lint

**PASS** — 0 errors, **1 warning** (pre-existing, intentional):
- `src/hooks/use-game-engine.ts:490` — `react-hooks/exhaustive-deps` missing `inputRef`.
  Intentionally excluded with an explanatory comment (including it would restart the game loop).
  Out of scope for this spec; not a regression target.

## Summary

The codebase is **green at baseline**. The stabilization work must keep it green (668 tests,
clean build, 0 lint errors) and must not introduce new failures or warnings.
